import { describe, expect, it } from 'vitest'
import { existsSync, readFileSync, readdirSync } from 'node:fs'
import { join } from 'node:path'
import { whyTrustTabs } from '../data/why'

const root = process.cwd()

function readComponent(relativePath: string) {
  return readFileSync(join(root, relativePath), 'utf8')
}

function listVueFiles(directory: string): string[] {
  return readdirSync(join(root, directory), { withFileTypes: true }).flatMap((entry) => {
    const path = `${directory}/${entry.name}`

    if (entry.isDirectory()) {
      return listVueFiles(path)
    }

    return entry.name.endsWith('.vue') ? [path] : []
  })
}

describe('visual implementation contract', () => {
  it('uses existing visual assets for the first viewport and solution cards', () => {
    const page = readComponent('pages/index.vue')
    const hero = readComponent('components/home/HomeHero.vue')
    expect(hero).not.toContain('HeroTresBackground')
    expect(hero).not.toContain('TresCanvas')
    expect(page).toContain('class="home-hero-deliverables__video"')
    expect(page).toContain('花瓣素材_+暖蓝光泄漏背景可循环')

    for (const asset of [
      'public/images/home/solutions/industrial.K00G2HaS.png',
      'public/images/home/solutions/smart-env.CWc2pooP.png',
      'public/images/home/solutions/smart-energy.DHKY-NE1.png',
      'public/images/home/solutions/smart-Water.DHKY-NE1.png',
      'public/images/home/solutions/data-center-ai.CDu93Miw.png',
      'public/images/home/solutions/data.DHKY-NE1.png',
    ]) {
      expect(existsSync(join(root, asset))).toBe(true)
    }
  })

  it('keeps component styles in SCSS, avoids inline style attributes, and caps component size', () => {
    const vueFiles = [...listVueFiles('components'), ...listVueFiles('pages')]

    for (const file of vueFiles) {
      const source = readComponent(file)
      const sourceWithoutRequiredNavWrapper = source.replace(' style="position:relative;"', '')
      const lineCount = source.split(/\r?\n/).length

      if (file.startsWith('components/') && source.includes('<style')) {
        expect(source).toContain('<style scoped lang="scss">')
      }

      expect(lineCount, `${file} exceeds 300 lines`).toBeLessThanOrEqual(300)
      expect(sourceWithoutRequiredNavWrapper).not.toMatch(/\sstyle=/)
    }
  })

  it('includes responsive navigation and mobile product fallback', () => {
    expect(readComponent('components/navigation/SiteHeaderMobileNav.vue')).toContain('mobile-navigation')
    expect(readComponent('components/home/HomeProductSystemMobileFlow.vue')).toContain('product-system__mobile-flow')
  })

  it('keeps the DGP product page aligned with its requirement document', () => {
    const page = readComponent('pages/products/data-governance.vue')
    const pageHero = readComponent('components/common/PageHero.vue')
    const hero = readComponent('components/product/dgp/DgpHero.vue')
    const heroVisual = readComponent('components/product/dgp/DgpHeroVisual.vue')
    const featureGrid = readComponent('components/common/ProductFeatureGridSection.vue')
    const sectionHeader = readComponent('components/common/section/SectionHeader.vue')
    const sectionShell = readComponent('components/common/section/SectionShell.vue')
    const baseCard = readComponent('components/common/card/BaseCard.vue')
    const iconBox = readComponent('components/common/card/IconBox.vue')
    const cardGrid = readComponent('components/common/card/CardGrid.vue')
    const featureCard = readComponent('components/common/card/FeatureCard.vue')
    const baseTabs = readComponent('components/common/tabs/BaseTabs.vue')
    const architecture = readComponent('components/product/dgp/DgpArchitecture.vue')
    const systemCards = readComponent('components/common/SystemCards.vue')
    const evolution = readComponent('components/product/dgp/DgpEvolutionSection.vue')
    const useCases = readComponent('components/product/dgp/DgpUseCasesSection.vue')
    const data = readComponent('data/dgp.ts')

    expect(page).toContain('SiteHeader')
    expect(page).toContain('SiteFooter')
    expect(page).toContain('DgpHero')
    expect(page).toContain('ProductFeatureGridSection')
    expect(page).toContain('DgpArchitecture')
    expect(page).toContain('DgpEvolutionSection')
    expect(page).toContain('DgpUseCasesSection')
    expect(page).toContain('CtaSection')
    expect(page).toContain('为什么选择数曜·治理数据平台')
    expect(page).toContain('开启企业数据治理新征程')
    expect(page).toContain('title-id="dgp-cta-title"')
    expect(page).toContain('nowrap-subtitle')
    expect(page).not.toContain('top-padding')

    expect(hero).toContain('PageHero')
    expect(hero).toContain('import { Database }')
    expect(hero).toContain('badge="数曜·数据治理平台"')
    expect(hero).toContain('title-line="可用、可管、可信"')
    expect(hero).toContain('title-gradient="企业数据底座"')
    expect(hero).toContain('visual-label="SHUYAODGP_HORE_WEBM"')
    expect(hero).toContain('DgpHeroVisual')
    expect(hero).toContain('visual-size="large"')
    expect(pageHero).toContain("visualSize: 'default'")
    expect(pageHero).toContain("'max-w-lg self-center'")
    expect(pageHero).toContain("'max-w-[820px] self-stretch'")

    expect(heroVisual).toContain('数曜·数据治理平台')
    expect(heroVisual).toContain('Intelligent')
    expect(heroVisual).toContain('Governed Data')
    expect(heroVisual).toContain('dgpGovernanceScenes')
    expect(heroVisual).toContain('bg-dt-bg-soft/35')
    expect(heroVisual).toContain('sm:grid-cols-[minmax(0,0.8fr)_minmax(0,1.2fr)]')
    expect(heroVisual).toContain('motion-safe:animate-pulse')
    expect(heroVisual).not.toContain('<style')
    expect(heroVisual).not.toContain('esenruizhi.com')

    expect(featureGrid).toContain('SectionShell')
    expect(featureGrid).toContain('SectionHeader')
    expect(featureGrid).toContain('CardGrid')
    expect(featureGrid).toContain('FeatureCard')
    expect(sectionShell).toContain('pb-32 lg:pb-44')
    expect(sectionShell).toContain('bg-dt-bg')
    expect(featureGrid).not.toContain('pt-24')
    expect(cardGrid).toContain('md:grid-cols-2 lg:grid-cols-4')
    expect(featureGrid).toContain(':title-id="titleId"')
    expect(cardGrid).toContain('auto-rows-fr items-stretch')
    expect(featureCard).toContain('BaseCard')
    expect(featureCard).toContain('IconBox')
    expect(featureCard).toContain('CardText')
    expect(baseCard).toContain('dt-card--feature')
    expect(baseCard).toContain('dt-card--adaptive')
    expect(baseCard).toContain('dt-card__accent')
    expect(iconBox).toContain('dt-icon-box')
    expect(iconBox).toContain('dt-icon-box--gradient')
    expect(featureGrid).not.toContain('mb-6')
    expect(featureCard).toContain('iconSize: 20')
    expect(featureCard).toContain("titleSize: 'sm'")
    expect(featureCard).toContain("descriptionSize: 'sm'")
    expect(featureGrid).not.toContain('text-base leading-relaxed')
    expect(featureGrid).not.toContain('min-h-[')
    expect(featureGrid).not.toContain('<style')

    expect(architecture).toContain('ProductSystemSection')
    expect(architecture).toContain('ProductSystemFlowFrame')
    expect(architecture).toContain('产品架构图占位符')
    expect(architecture).toContain('content-flush')
    expect(architecture).not.toContain(':heading-wide="false"')
    expect(architecture).not.toContain(':nowrap-subtitle="false"')
    expect(architecture).toContain('SystemCards')
    expect(architecture).not.toContain('EnterpriseFlow')

    expect(systemCards).toContain('CardGrid')
    expect(systemCards).toContain('FeatureCard')
    expect(systemCards).toContain('columns="three" gap="sm"')
    expect(systemCards).toContain('mt-10 lg:mt-12')
    expect(systemCards).toContain('dt-icon-box dt-icon-box--gradient')
    expect(cardGrid).toContain('md:grid-cols-3')
    expect(iconBox).toContain('dt-icon-box')
    expect(systemCards).not.toContain('rounded-2xl border border-dt-line')
    expect(systemCards).not.toContain('hover:border-dt-primary/40')
    expect(systemCards).not.toContain('min-h-[')
    expect(systemCards).not.toContain('<style')

    expect(evolution).toContain('企业数据治理体系的演进')
    expect(evolution).toContain('class="container pb-32 lg:pb-44"')
    expect(evolution).toContain('SectionHeader')
    expect(evolution).not.toContain('eyebrow-size="sm"')
    expect(evolution).not.toContain('eyebrow-tone="primary"')
    expect(evolution).toContain('nowrap-subtitle')
    expect(sectionHeader).toContain('section-heading--eyebrow-sm')
    expect(sectionHeader).toContain('section-heading--eyebrow-primary')
    expect(sectionHeader).toContain('section-heading--nowrap-subtitle')
    expect(evolution).toContain('class="py-4 lg:py-8"')
    expect(evolution).not.toContain('class="dt-card p-6 backdrop-blur-xl"')
    expect(evolution).toContain('rounded-full border border-primary/20 bg-primary/5 px-2.5 py-1 text-[13px] font-semibold text-primary/80')
    expect(evolution).toContain('lg:grid-cols-2 lg:gap-12')
    expect(evolution).toContain('图片占位符')
    expect(evolution).not.toContain('border border-dt-line bg-dt-bg-soft/40')
    expect(evolution).not.toContain('SectionHeading')
    expect(evolution).not.toContain('<style')

    expect(useCases).toContain('class="pb-32 lg:pb-44"')
    expect(useCases).toContain('text-center text-4xl font-bold leading-[1.2] tracking-tight text-highlighted')
    expect(useCases).toContain('text-center text-base text-default')
    expect(useCases).toContain('lg:whitespace-nowrap')
    expect(useCases).toContain('BaseTabs')
    expect(useCases).toContain('useCaseTabs')
    expect(useCases).toContain('variant="underline"')
    expect(baseTabs).toContain('role="tablist"')
    expect(baseTabs).toContain('role="tab"')
    expect(baseTabs).toContain('data-slot="root"')
    expect(baseTabs).toContain('data-slot="list"')
    expect(baseTabs).toContain('data-slot="trigger"')
    expect(baseTabs).toContain('data-slot="indicator"')
    expect(baseTabs).toContain('data-slot="label"')
    expect(baseTabs).toContain('px-5 py-4 text-base font-medium')
    expect(baseTabs).toContain('border-b border-dt-line')
    expect(useCases).toContain('role="tabpanel"')
    expect(useCases).toContain('BaseButton')
    expect(useCases).toContain('了解更多')
    expect(useCases).not.toContain('class="dt-tab"')
    expect(useCases).not.toContain('SectionHeading')
    expect(useCases).not.toContain('!pb-0')
    expect(useCases).not.toContain('<style')

    for (const text of [
      '让数据快速可用',
      '数据接入',
      '数据集成',
      '政务数据治理',
      '物联数据汇聚与治理',
    ]) {
      expect(data).toContain(text)
    }
  })

  it('keeps the DLP product page aligned with its requirement document', () => {
    const page = readComponent('pages/products/data-labeling.vue')
    const hero = readComponent('components/product/dlp/DlpHero.vue')
    const heroVisual = readComponent('components/product/dlp/DlpHeroVisual.vue')
    const architecture = readComponent('components/product/dlp/DlpArchitecture.vue')
    const timeline = readComponent('components/product/dlp/DlpCapabilityTimelineSection.vue')
    const alternatingTimeline = readComponent('components/common/AlternatingTimelineSection.vue')
    const aiModeling = readComponent('components/product/dlp/DlpAiModelingSection.vue')
    const featureGrid = readComponent('components/common/ProductFeatureGridSection.vue')
    const cardGrid = readComponent('components/common/card/CardGrid.vue')
    const data = readComponent('data/dlp.ts')

    expect(page).toContain('SiteHeader')
    expect(page).toContain('SiteFooter')
    expect(page).toContain('DlpHero')
    expect(page).toContain('ProductFeatureGridSection')
    expect(page).toContain('DlpArchitecture')
    expect(page).toContain('DlpCapabilityTimelineSection')
    expect(page).toContain('DlpAiModelingSection')
    expect(page).toContain('CtaSection')
    expect(page).toContain('id="dlp-challenge"')
    expect(page).toContain('eyebrow="挑战"')
    expect(page).toContain('title="厌倦了低效的标签建设？"')
    expect(page).toContain('title="构建统一的企业标签体系"')
    expect(page).toContain('title="从数据对象到标签服务"')
    expect(page).toContain('title="为企业 AI 场景赋能"')
    expect(page).toContain(':items="dlpAdvantages"')
    expect(page).toContain('columns="two"')
    expect(page).toContain('columns="three"')
    expect(page).toContain('title-id="dlp-cta-title"')
    expect(page).not.toContain('<style')

    expect(hero).toContain('PageHero')
    expect(hero).toContain('import { Boxes }')
    expect(hero).toContain('badge="数曜·数据标签平台"')
    expect(hero).toContain('title-line="协同、智能、高效"')
    expect(hero).toContain('title-gradient="标签生产平台"')
    expect(hero).toContain('visual-label="SHUYAODGP_HORE_WEBM"')
    expect(hero).toContain('DlpHeroVisual')
    expect(hero).toContain('visual-size="large"')
    const heroSql = readComponent('components/product/dlp/heroSql.ts')
    expect(heroVisual).toContain('数曜·数据标签平台')
    expect(heroVisual).toContain('标签生成')
    expect(heroVisual).toContain('标签查询')
    expect(heroVisual).toContain('实时标签生成')
    expect(heroVisual).toContain('tag_results')
    expect(heroVisual).toContain('h-[475px]')
    expect(heroVisual).toContain('grid-cols-4')
    expect(heroVisual).toContain('正在生成标签...')
    expect(heroVisual).toContain('标签已生成，可直接查询、分析与服务调用。')
    expect(heroVisual).toContain('标签实时查询')
    expect(heroVisual).toContain('主体画像')
    expect(heroVisual).toContain('查询标签')
    expect(heroVisual).toContain('正在查询标签...')
    expect(heroVisual).toContain('已找到 3 个标签，可用于分析、分群与服务调用。')
    expect(heroVisual).toContain('animate-glow')
    expect(heroVisual).toContain('dlpHeroFeatures')
    expect(heroVisual).toContain('dlpHeroTagResults')
    expect(heroVisual).toContain('dlpHeroQueryResults')
    expect(heroVisual).not.toContain('BaseTabs')
    expect(heroVisual).not.toContain('图片占位符')
    expect(heroVisual).not.toContain('<style')
    expect(heroSql).toContain('tag_results')
    expect(heroSql).toContain("customer_id = 'A1024'")
    expect(heroSql).toContain('tokenizeSql')

    expect(architecture).toContain('ProductSystemSection')
    expect(architecture).toContain('ProductSystemFlowFrame')
    expect(architecture).toContain('title="轻松构建企业标签体系"')
    expect(architecture).toContain('fallback-text="标签体系架构图占位符"')
    expect(architecture).toContain('content-flush')
    expect(architecture).not.toContain('EnterpriseFlow')
    expect(architecture).not.toContain('<style')

    expect(featureGrid).toContain("columns?: 'two' | 'three' | 'four'")
    expect(cardGrid).toContain('md:grid-cols-3')
    expect(timeline).toContain('AlternatingTimelineSection')
    expect(timeline).not.toContain('BaseCard')
    expect(timeline).not.toContain('IconBox')
    expect(timeline).toContain('dlpTimelineItems')
    expect(alternatingTimeline).toContain('SectionHeader')
    expect(alternatingTimeline).toContain('lg:left-1/2')
    expect(alternatingTimeline).toContain('space-y-[4.5rem]')
    expect(alternatingTimeline).toContain('class="py-4 lg:py-8"')
    expect(alternatingTimeline).toContain('rounded-full border border-primary/20 bg-primary/5 px-2.5 py-1 text-[13px] font-semibold text-primary/80')
    expect(alternatingTimeline).not.toContain('BaseCard')
    expect(alternatingTimeline).not.toContain('IconBox')
    expect(timeline).not.toContain('item.icon')
    expect(timeline).not.toContain('eyebrow-size="sm"')
    expect(timeline).not.toContain('eyebrow-tone="primary"')
    expect(alternatingTimeline).toContain('role="img"')
    expect(alternatingTimeline).toContain('图片占位符')
    expect(alternatingTimeline).not.toContain('<style')
    expect(timeline).not.toContain('<style')
    expect(aiModeling).toContain('SectionHeader')
    expect(aiModeling).toContain('AI 辅助建标')
    expect(aiModeling).toContain('图片占位符')
    expect(aiModeling).not.toContain('<style')

    for (const text of [
      '标准混乱',
      '自动化生产',
      '统一定义标签标准',
      '主体对象自由定义',
      '精准用户运营',
      '立即咨询',
      'customer/visit_count',
      'customer/order_amount',
      'customer/last_active',
      '高活跃',
      '高价值',
      '近期活跃',
    ]) {
      expect(data).toContain(text)
    }
  })

  it('keeps the DDP product page aligned with its requirement document', () => {
    const page = readComponent('pages/products/data-development.vue')
    const pageHero = readComponent('components/common/PageHero.vue')
    const hero = readComponent('components/product/ddp/DdpHero.vue')
    const heroVisual = readComponent('components/product/ddp/DdpHeroVisual.vue')
    const heroSql = readComponent('components/product/ddp/ddpHeroSql.ts')
    const heroAnimation = readComponent('components/product/ddp/useDdpHeroAnimation.ts')
    const flowCanvas = readComponent('components/product/ddp/DdpFlowCanvas.client.vue')
    const flowNode = readComponent('components/product/ddp/DdpFlowNode.vue')
    const architecture = readComponent('components/product/ddp/DdpArchitecture.vue')
    const timeline = readComponent('components/product/ddp/DdpCapabilityTimelineSection.vue')
    const unified = readComponent('components/product/ddp/DdpUnifiedDevelopmentSection.vue')
    const alternatingTimeline = readComponent('components/common/AlternatingTimelineSection.vue')
    const featureGrid = readComponent('components/common/ProductFeatureGridSection.vue')
    const featureCard = readComponent('components/common/card/FeatureCard.vue')
    const cardGrid = readComponent('components/common/card/CardGrid.vue')
    const data = readComponent('data/ddp.ts')

    expect(page).toContain('SiteHeader')
    expect(page).toContain('SiteFooter')
    expect(page).toContain('DdpHero')
    expect(page).toContain('ProductFeatureGridSection')
    expect(page).toContain('DdpArchitecture')
    expect(page).toContain('DdpCapabilityTimelineSection')
    expect(page).toContain('DdpUnifiedDevelopmentSection')
    expect(page).toContain('CtaSection')
    expect(page).toContain('id="ddp-challenge"')
    expect(page).toContain('title="数据开发，正在成为企业增长瓶颈"')
    expect(page).toContain('title="持续释放企业数据生产力"')
    expect(page).toContain('title="赋能企业数据工程实践"')
    expect(page).toContain('columns="two"')
    expect(page).toContain('columns="three"')
    expect(page).toContain('title-id="ddp-cta-title"')
    expect(page).not.toContain('<style')

    expect(hero).toContain('PageHero')
    expect(hero).toContain('import { Network }')
    expect(hero).toContain('badge="数曜·数据开发平台"')
    expect(hero).toContain('title-line="标准、智能、高效"')
    expect(hero).toContain('title-gradient="数据开发平台"')
    expect(hero).toContain('visual-label="SHUYAODDP_HORE_WEBM"')
    expect(hero).toContain('DdpHeroVisual')
    expect(hero).toContain('visual-size="fluid"')
    expect(hero).not.toContain('visual-size="large"')
    expect(pageHero).toContain("visualSize?: 'default' | 'large' | 'fluid'")
    expect(pageHero).toContain('lg:flex-row lg:items-center lg:justify-between')
    expect(pageHero).toContain('max-w-lg xl:max-w-xl 2xl:max-w-2xl')
    expect(heroVisual).not.toContain('图片占位符')
    expect(heroVisual).not.toContain('<style')
    expect(heroVisual).toContain('SQL 开发')
    expect(heroVisual).toContain('可视化编排')
    expect(heroVisual).toContain('h-[368px]')
    expect(heroVisual).toContain('<ClientOnly>')
    expect(heroVisual).toContain('bg-gradient-to-br from-primary/20 via-transparent to-primary/15 blur-2xl')
    expect(heroVisual).toContain('rounded-2xl border border-muted bg-default shadow-2xl')
    expect(heroVisual).toContain('size-3 rounded-full bg-red-500/70')
    expect(heroVisual).toContain('animate-pulse bg-primary')
    expect(heroVisual).toContain('DdpFlowCanvas')
    expect(heroSql).toContain("text: 'INSERT OVERWRITE TABLE '")
    expect(heroSql).toContain("text: 'dws_user_order_summary'")
    expect(heroSql).toContain("text: 'PARTITION '")
    expect(heroSql).toContain('${bizdate}')
    expect(heroSql).toContain("text: 'GROUP BY '")
    expect(heroSql).toContain("text: 'dwd_order_detail '")
    expect(heroSql).toContain("text: 'dim_user_info '")
    expect(heroSql).toContain('text-blue-500 dark:text-blue-400')
    expect(heroSql).toContain('text-emerald-600 dark:text-emerald-400')
    expect(heroSql).toContain('text-amber-600 dark:text-amber-400')
    expect(heroAnimation).toContain('LINE_REVEAL_INTERVAL_MS = 300')
    expect(heroAnimation).toContain('TAB_SWITCH_INTERVAL_MS = 5000')
    expect(flowCanvas).toContain('@vue-flow/core')
    expect(flowCanvas).toContain('DWD Detail')
    expect(flowCanvas).toContain('dwd_order_detail')
    expect(flowCanvas).toContain('Data Transform')
    expect(flowCanvas).toContain('join · aggregate')
    expect(flowCanvas).toContain('DIM Join')
    expect(flowCanvas).toContain('dim_user_info')
    expect(flowCanvas).toContain('DWS Summary')
    expect(flowCanvas).toContain('dws_user_order_summary')
    expect(flowCanvas).toContain('ADS Application')
    expect(flowCanvas).toContain('ads_user_value')
    expect(flowCanvas).toContain('rgba(148, 163, 184, 0.7)')
    expect(flowCanvas).not.toContain('MQTT')
    expect(flowCanvas).not.toContain('Kafka')
    expect(flowCanvas).not.toContain('PostgreSQL')
    expect(flowNode).toContain('min-w-[190px]')
    expect(flowNode).toContain('bg-emerald-500')
    expect(flowNode).toContain('bg-indigo-500')
    expect(flowNode).toContain('bg-purple-500')
    expect(flowNode).not.toContain('<style')

    expect(architecture).toContain('ProductSystemSection')
    expect(architecture).toContain('ProductSystemFlowFrame')
    expect(architecture).toContain('title="构建智能数据开发体系"')
    expect(architecture).toContain('fallback-text="数据开发体系架构图占位符"')
    expect(architecture).toContain('content-flush')
    expect(architecture).not.toContain('EnterpriseFlow')
    expect(architecture).not.toContain('<style')

    expect(timeline).toContain('AlternatingTimelineSection')
    expect(timeline).toContain('ddpTimelineItems')
    expect(timeline).toContain('title="覆盖数据开发全生命周期"')
    expect(timeline).not.toContain('BaseCard')
    expect(timeline).not.toContain('IconBox')
    expect(timeline).not.toContain('<style')
    expect(alternatingTimeline).toContain('SectionHeader')
    expect(alternatingTimeline).toContain('lg:left-1/2')
    expect(alternatingTimeline).toContain('space-y-[4.5rem]')
    expect(alternatingTimeline).toContain('role="img"')
    expect(alternatingTimeline).not.toContain('<style')

    expect(unified).toContain('ProductFeatureGridSection')
    expect(unified).toContain('ProductSystemFlowFrame')
    expect(unified).toContain('title="统一数据开发：从接入到交付"')
    expect(unified).toContain('ddpUnifiedDevelopmentItems')
    expect(unified).toContain('fallback-text="统一数据开发流程图占位符"')
    expect(unified).not.toContain('<style')
    expect(featureGrid).toContain('<slot name="after" />')
    expect(featureGrid).toContain('<slot name="before" />')
    expect(featureGrid).toContain(':icon-label="item.iconLabel"')
    expect(featureCard).toContain('iconLabel?: string')
    expect(featureCard).toContain('dt-icon-box dt-icon-box--gradient text-sm font-bold')
    expect(cardGrid).toContain('md:grid-cols-2 lg:grid-cols-4')
    expect(cardGrid).toContain('md:grid-cols-3')

    for (const text of [
      '数曜·数据开发平台',
      '数据开发，正在成为企业增长瓶颈',
      '构建智能数据开发体系',
      '覆盖数据开发全生命周期',
      '统一数据开发：从接入到交付',
      '赋能企业数据工程实践',
      '开启企业数据治理新征程',
    ]) {
      expect([page, hero, architecture, timeline, unified, data].join('\n')).toContain(text)
    }
  })

  it('keeps the DMS product page aligned with its requirement document', () => {
    const page = readComponent('pages/products/data-element-regulation.vue')
    const hero = readComponent('components/product/dms/DmsHero.vue')
    const heroVisual = readComponent('components/product/dms/DmsHeroVisual.vue')
    const heroAnimation = readComponent('components/product/dms/useDmsHeroAnimation.ts')
    const architecture = readComponent('components/product/dms/DmsArchitecture.vue')
    const intelligentRegulation = readComponent('components/product/dms/DmsIntelligentRegulationSection.vue')
    const businessValue = readComponent('components/product/dms/DmsBusinessValueSection.vue')
    const regulationProcess = readComponent('components/product/dms/DmsRegulationProcessSection.vue')
    const featureGrid = readComponent('components/common/ProductFeatureGridSection.vue')
    const data = readComponent('data/dms.ts')

    expect(page).toContain('SiteHeader')
    expect(page).toContain('SiteFooter')
    expect(page).toContain('DmsHero')
    expect(page).toContain('ProductFeatureGridSection')
    expect(page).toContain('DmsArchitecture')
    expect(page).toContain('DmsIntelligentRegulationSection')
    expect(page).toContain('DmsBusinessValueSection')
    expect(page).toContain('DmsRegulationProcessSection')
    expect(page).toContain('CtaSection')
    expect(page).toContain('id="dms-challenge"')
    expect(page).toContain('title="数据流通不断扩大 监管能力亟需升级"')
    expect(page).toContain('title="要素全流程监管能力"')
    expect(page).toContain('title="赋能多场景数据要素监管"')
    expect(page).toContain('columns="three"')
    expect(page).toContain('columns="two"')
    expect(page).toContain('title-id="dms-cta-title"')
    expect(page).not.toContain('<style')

    expect(hero).toContain('PageHero')
    expect(hero).toContain('import { HardDrive }')
    expect(hero).toContain('badge="数曜·数据要素监管平台"')
    expect(hero).toContain('title-line="让数据流通安全、可信"')
    expect(hero).toContain('title-gradient="全流程监管平台"')
    expect(hero).toContain('visual-label="SHUYAODMS_HORE_WEBM"')
    expect(hero).toContain('visual-size="large"')
    expect(hero).toContain('DmsHeroVisual')
    expect(heroVisual).toContain('dmsHeroTitle')
    expect(heroVisual).toContain('dmsHeroTabs')
    expect(heroVisual).toContain('dmsHeroIntakeFields')
    expect(heroVisual).toContain('dmsHeroRiskRules')
    expect(heroVisual).toContain('dmsHeroRiskSummary')
    expect(heroVisual).toContain('dmsHeroDisposalFields')
    expect(heroVisual).toContain('dmsHeroDisposalSteps')
    expect(heroVisual).toContain('useDmsHeroAnimation')
    expect(heroVisual).toContain('getProgressWidthClass')
    expect(heroVisual).toContain('relative h-[320px]')
    expect(heroVisual).toContain('flex gap-1.5 border-t border-muted px-5 py-3')
    expect(heroVisual).not.toContain('<style')
    expect(heroVisual).not.toContain('style=')
    expect(heroAnimation).toContain('STAGE_DURATION_MS = 5000')
    expect(heroAnimation).toContain('PROGRESS_WIDTH_CLASSES')
    expect(heroAnimation).toContain('getProgressWidthClass')
    expect(heroAnimation).toContain('INTAKE_STEP_INTERVAL_MS = 800')
    expect(heroAnimation).toContain('RISK_SCAN_START_MS = 700')
    expect(heroAnimation).toContain('RISK_SCAN_INTERVAL_MS = 500')
    expect(heroAnimation).toContain('DISPOSAL_STEP_START_MS = 900')
    expect(heroAnimation).toContain('DISPOSAL_STEP_INTERVAL_MS = 800')
    expect(heroAnimation).toContain('DISPOSAL_DONE_DELAY_MS = 4100')
    expect(data).toContain('dmsHeroTitle')
    expect(data).toContain('dmsHeroTabs')
    expect(data).toContain('TX-20260811-042')
    expect(data).toContain('企业经营分析数据集')
    expect(data).toContain('敏感字段超范围')
    expect(data).toContain('EVT-20260811-017')
    expect(data).toContain('监管工单已创建')

    expect(architecture).toContain('ProductSystemSection')
    expect(architecture).toContain('ProductSystemFlowFrame')
    expect(architecture).toContain('title="构建数据要素流通全过程监管体系"')
    expect(architecture).toContain('fallback-text="数据要素监管体系架构图占位符"')
    expect(architecture).toContain('content-flush')
    expect(architecture).not.toContain('EnterpriseFlow')
    expect(architecture).not.toContain('<style')

    expect(intelligentRegulation).toContain('AlternatingTimelineSection')
    expect(intelligentRegulation).toContain('dmsTimelineItems')
    expect(intelligentRegulation).toContain('title="智能监管，全程守护"')
    expect(intelligentRegulation).not.toContain('BaseCard')
    expect(intelligentRegulation).not.toContain('IconBox')
    expect(intelligentRegulation).not.toContain('<style')

    expect(businessValue).toContain('ProductSystemSection')
    expect(businessValue).toContain('eyebrow="业务价值"')
    expect(businessValue).toContain('title="可量化的数据要素监管效能"')
    expect(businessValue).toContain('title-id="dms-business-value-title"')
    expect(businessValue).toContain('dmsValueItems')
    expect(businessValue).toContain('grid gap-5 md:grid-cols-2')
    expect(businessValue).toContain('group relative overflow-hidden rounded-2xl border border-default bg-default p-7 shadow-sm transition-shadow duration-500 hover:shadow-md lg:p-9')
    expect(businessValue).toContain('pointer-events-none absolute inset-x-0 top-0 h-px bg-gradient-to-r from-transparent via-primary/40 to-transparent')
    expect(businessValue).toContain('text-4xl font-bold tracking-tight text-primary lg:text-5xl')
    expect(businessValue).toContain('mt-4 text-sm leading-relaxed text-muted')
    expect(businessValue).toContain('mt-5 text-[13px] italic text-dimmed/60')
    expect(businessValue).toContain('item.detail')
    expect(businessValue).not.toContain('<style')

    expect(regulationProcess).toContain('ProductFeatureGridSection')
    expect(regulationProcess).toContain('ProductSystemFlowFrame')
    expect(regulationProcess).toContain('dmsProcessItems')
    expect(regulationProcess).toContain('columns="three"')
    expect(regulationProcess).toContain('<template #before>')
    expect(regulationProcess).toContain('fallback-text="数据要素监管流程图占位符"')
    expect(regulationProcess).not.toContain('<style')
    expect(featureGrid).toContain('<slot name="before" />')

    for (const text of [
      '数曜·数据要素监管平台',
      '数据流通不断扩大 监管能力亟需升级',
      '构建数据要素流通全过程监管体系',
      '要素全流程监管能力',
      '智能监管，全程守护',
      '可量化的数据要素监管效能',
      '事前预防 → 事中监控 → 事后处置 → 监管分析',
      '赋能多场景数据要素监管',
      '开启智能化数据要素监管',
    ]) {
      expect([page, hero, architecture, intelligentRegulation, businessValue, regulationProcess, data].join('\n')).toContain(text)
    }
  })

  it('keeps the Why DeepTrols page on the shared HOME baseline', () => {
    const page = readComponent('pages/why-deeptrols.vue')
    const pageHero = readComponent('components/common/PageHero.vue')
    const heroLogoStrip = readComponent('components/common/HeroLogoStrip.vue')
    const trustTabsSection = readComponent('components/common/TrustTabsSection.vue')
    const serviceShowcaseSection = readComponent('components/common/ServiceShowcaseSection.vue')
    const engineLinksSection = readComponent('components/common/EngineLinksSection.vue')
    const sectionShell = readComponent('components/common/section/SectionShell.vue')
    const baseCard = readComponent('components/common/card/BaseCard.vue')
    const iconBox = readComponent('components/common/card/IconBox.vue')
    const cardGrid = readComponent('components/common/card/CardGrid.vue')
    const featureCard = readComponent('components/common/card/FeatureCard.vue')
    const cardText = readComponent('components/common/card/CardText.vue')
    const baseTabs = readComponent('components/common/tabs/BaseTabs.vue')
    const hero = readComponent('components/why/WhyHero.vue')
    const heroLogos = readComponent('components/why/WhyHeroLogos.vue')
    const heroVisual = readComponent('components/why/WhyHeroVisual.vue')
    const heroAnimation = readComponent('components/why/useWhyHeroAnimation.ts')
    const tailwindCss = readComponent('assets/css/tailwind.css')
    const tabs = readComponent('components/why/WhyTrustTabs.vue')
    const service = readComponent('components/why/WhyServiceReset.vue')
    const engine = readComponent('components/why/WhyEngine.vue')
    const data = readComponent('data/why.ts')

    expect(page).toContain('SiteHeader')
    expect(page).toContain('SiteFooter')
    expect(page).toContain('WhyHero')
    expect(page).toContain('WhyHeroLogos')
    expect(page).toContain('WhyTrustTabs')
    expect(page).toContain('WhyServiceReset')
    expect(page).toContain('WhyEngine')
    expect(page).toContain('CtaSection')
    expect(page).toContain('whyCtaActions')
    expect(hero).toContain('PageHero')
    expect(hero).toContain('import { Sparkles }')
    expect(hero).toContain('badge="为什么选择 DeepTrols"')
    expect(hero).toContain('title-id="why-hero-title"')
    expect(hero).toContain('title-line="数据、知识、智能统一"')
    expect(hero).toContain('title-gradient="企业级AI应用服务商"')
    expect(hero).toContain('visual-size="large"')
    expect(hero).not.toContain('HomeCustomerLogos')
    expect(hero).not.toContain('why-hero__logos')
    expect(heroLogos).toContain('HeroLogoStrip')
    expect(heroLogos).toContain('HomeCustomerLogos')
    expect(hero).toContain('flush-bottom')
    expect(heroLogoStrip).toContain('class="container"')
    expect(heroLogoStrip).toContain('class="hero-logo-strip__inner mt-24 border-t border-dt-line pt-12 lg:mt-28"')
    expect(heroLogoStrip).toContain('padding-bottom: 96px')
    expect(heroLogoStrip).toContain('padding-bottom: 128px')
    expect(pageHero).toContain('class="page-hero relative overflow-hidden bg-dt-bg"')
    expect(pageHero).toContain('container page-hero__body relative pt-24 lg:pt-32')
    expect(pageHero).toContain("flushBottom ? 'pb-0 lg:pb-0' : 'pb-24 lg:pb-32'")
    expect(pageHero).toContain('page-hero__content w-full text-center')
    expect(pageHero).toContain("align === 'center' ? 'max-w-4xl' : 'max-w-2xl lg:self-center lg:text-left'")
    expect(pageHero).toContain('page-hero__actions flex flex-wrap items-center justify-center gap-4')
    expect(pageHero).toContain("align === 'center' ? '' : 'lg:justify-start'")
    expect(pageHero).not.toContain('padding: 144px')
    expect(pageHero).not.toContain('padding: 144px 0 96px')
    expect(pageHero).not.toContain('padding-bottom: 112px')
    expect(pageHero).toContain('font-size: 36px')
    expect(pageHero).toContain('font-size: 48px')
    expect(pageHero).toContain('font-size: 60px')
    expect(pageHero).toContain('line-height: 1.1')
    expect(pageHero).toContain('margin: 0 auto 32px')
    expect(hero).toContain('立即咨询')
    expect(hero).toContain('关于我们')
    expect(pageHero).toContain('BaseButton')
    expect(hero).toContain('WhyHeroVisual')
    expect(hero).not.toContain('<video')
    expect(hero).not.toContain('robot.webm')
    expect(hero).not.toContain('new URL(')
    expect(pageHero).toContain('background-size: 72px 72px')
    expect(pageHero).toContain('max-w-[820px]')
    expect(pageHero).toContain('self-stretch')
    expect(hero).toContain('lg:h-full')
    expect(hero).toContain('max-w-[728px]')
    expect(hero).not.toContain('&::after')
    expect(hero).not.toContain('linear-gradient(180deg, var(--dt-color-bg) 0%')
    expect(hero).not.toContain('linear-gradient(90deg, var(--dt-color-bg) 0%')
    expect(pageHero).toContain('lg:grid-cols-[minmax(0,0.88fr)_minmax(520px,1fr)]')
    expect(hero).not.toContain('video {')
    expect(hero).not.toContain('box-shadow: 0 24px 60px')
    expect(heroVisual).toContain('viewBox="0 0 560 480"')
    expect(heroVisual).toContain('whyHeroNodes')
    expect(heroVisual).toContain('whyHeroCenterLogo')
    expect(heroVisual).toContain('useWhyHeroAnimation')
    expect(heroVisual).toContain('animate-why-node-pulse')
    expect(heroVisual).toContain('animate-why-center-pulse')
    expect(heroVisual).toContain('preserveAspectRatio="xMidYMid meet"')
    expect(heroVisual).toContain('rx="16"')
    expect(heroVisual).toContain('why-hv-hub-ring')
    expect(heroVisual).not.toContain('hubRingArcs')
    expect(heroVisual).toContain('fill-[var(--dt-color-bg-elevated)]')
    expect(heroAnimation).toContain('ROTATE_DURATION_MS = 800')
    expect(heroAnimation).toContain('SETTLE_DELAY_MS = 200')
    expect(heroAnimation).toContain('MESSAGE_DURATION_MS = 900')
    expect(heroAnimation).toContain('HOLD_DURATION_MS = 400')
    expect(heroAnimation).toContain('START_DELAY_MS = 400')
    expect(heroAnimation).toContain('INITIAL_ROTATION_DEG = 270')
    expect(heroAnimation).toContain('ROTATION_STEP_DEG = 90')
    expect(heroAnimation).toContain('ORBIT_RADIUS = 150')
    expect(tailwindCss).toContain('--animate-why-node-pulse')
    expect(tailwindCss).toContain('--animate-why-center-pulse')
    expect(data).toContain('whyHeroNodes')
    expect(data).toContain('whyHeroCenterLogo')
    expect(data).toContain('数曜logo.svg?url')
    expect(data).toContain('博曜logo.svg?url')
    expect(data).toContain('探曜IOT logo-1.svg?url')
    expect(data).toContain('智曜logo.svg?url')
    expect(data).toContain("whyHeroCenterLogo = '/logo-while.svg'")
    expect(tabs).toContain('TrustTabsSection')
    expect(tabs).toContain('为什么DeepTrols值得信赖')
    expect(trustTabsSection).toContain('SectionShell')
    expect(trustTabsSection).toContain('SectionHeader')
    expect(trustTabsSection).toContain('BaseTabs')
    expect(trustTabsSection).toContain('CardGrid')
    expect(trustTabsSection).toContain('BaseCard')
    expect(trustTabsSection).toContain('IconBox')
    expect(sectionShell).toContain('pb-32 lg:pb-44')
    expect(baseTabs).toContain('role="tablist"')
    expect(baseTabs).toContain('role="tab"')
    expect(trustTabsSection).toContain('role="tabpanel"')
    expect(trustTabsSection).toContain('mb-12 text-center lg:mb-16')
    expect(baseTabs).toContain('dt-segmented-tabs')
    expect(baseTabs).toContain('dt-segmented-tab')
    expect(cardGrid).toContain('gap-5 lg:gap-6')
    expect(cardGrid).toContain('md:grid-cols-2')
    expect(trustTabsSection).toContain('variant="product"')
    expect(trustTabsSection).toContain('equal-height')
    expect(trustTabsSection).toContain('!p-7 lg:!p-8')
    expect(baseCard).toContain('dt-product-card')
    expect(baseCard).toContain('dt-card--adaptive')
    expect(trustTabsSection).not.toContain('min-h-[280px]')
    expect(iconBox).toContain('dt-icon-box')
    expect(trustTabsSection).not.toContain('!rounded-xl')
    expect(trustTabsSection).toContain(':size="40"')
    expect(trustTabsSection).toContain(':icon-size="20"')
    expect(trustTabsSection).toContain('font-size: 24px')
    expect(trustTabsSection).toContain('font-size: 18px')
    expect(trustTabsSection).not.toContain('dt-tab-list')
    expect(trustTabsSection).not.toContain('class="dt-tab"')
    expect(trustTabsSection).not.toContain('grid-template-columns: repeat(4, minmax(0, 1fr))')
    expect(serviceShowcaseSection).toContain('SectionHeader')
    expect(serviceShowcaseSection).toContain('FeatureCard')
    expect(engineLinksSection).toContain('SectionHeader')
    expect(engineLinksSection).toContain('BaseCard')
    expect(engineLinksSection).toContain('CardText')
    expect(engineLinksSection).toContain('IconBox')
    expect(service).toContain('业务价值可衡量，AI成果可持续')
    expect(service).toContain('ServiceShowcaseSection')
    expect(service).toContain('fangangaishu@2x.png?url')
    expect(service).not.toContain('new URL(')
    expect(serviceShowcaseSection).toContain('SectionHeader')
    expect(serviceShowcaseSection).toContain('variant="soft"')
    expect(featureCard).toContain(':variant="variant"')
    expect(baseCard).toContain('dt-card--soft')
    expect(serviceShowcaseSection).not.toContain('transform: translateY(-4px)')
    expect(serviceShowcaseSection).not.toContain('border-color: rgba(94, 78, 255, 0.4)')
    expect(serviceShowcaseSection).not.toContain('border-radius: 24px')
    expect(engine).toContain('重塑引擎')
    expect(engine).toContain('whyEngineLinks')
    expect(engine).toContain('EngineLinksSection')
    expect(engineLinksSection).toContain('SectionHeader')
    expect(engineLinksSection).toContain('variant="soft"')
    expect(engineLinksSection).toContain('tone="white"')
    expect(baseCard).toContain('dt-card--soft')
    expect(cardText).toContain('card-text__title')
    expect(engineLinksSection).not.toContain('transform: translateY(-4px)')
    expect(engineLinksSection).not.toContain('border-color: rgba(94, 78, 255, 0.4)')
    expect(engineLinksSection).not.toContain('border-radius: 24px')
    expect(data).toContain("label: '面向技术层'")
    expect(data).toContain("label: '面向业务层'")
    expect(data).toContain("label: '面向服务层'")
    expect(data).toContain("label: '面向长期价值'")
    expect(whyTrustTabs).toHaveLength(4)
    expect(whyTrustTabs.every((tab) => tab.features.length === 4)).toBe(true)
    expect(data).toContain('DeepTrolsOPS企业AI引擎')
    expect(data).toContain('FDE企业AI服务指南')
    expect(existsSync(join(root, 'public/logo-while.svg'))).toBe(true)
    expect(existsSync(join(root, 'doc/product/PAGE_REQUIREMENTS/WhyDeepTrols/imgs/数曜logo.svg'))).toBe(true)
    expect(existsSync(join(root, 'doc/product/PAGE_REQUIREMENTS/WhyDeepTrols/imgs/博曜logo.svg'))).toBe(true)
    expect(existsSync(join(root, 'doc/product/PAGE_REQUIREMENTS/WhyDeepTrols/imgs/探曜IOT logo-1.svg'))).toBe(true)
    expect(existsSync(join(root, 'doc/product/PAGE_REQUIREMENTS/WhyDeepTrols/imgs/智曜logo.svg'))).toBe(true)
    expect(existsSync(join(root, 'doc/product/PAGE_REQUIREMENTS/WhyDeepTrols/imgs/fangangaishu@2x.png'))).toBe(true)
  })

  it('keeps EMQX-inspired homepage rhythm contracts', () => {
    const tailwind = readComponent('assets/css/tailwind.css')
    const tokens = readComponent('assets/scss/main.scss')
    const page = readComponent('pages/index.vue')
    const header = readComponent('components/navigation/SiteHeader.vue')
    const headerDesktopNav = readComponent('components/navigation/SiteHeaderDesktopNav.vue')
    const headerActions = readComponent('components/navigation/SiteHeaderActions.vue')
    const headerMobileNav = readComponent('components/navigation/SiteHeaderMobileNav.vue')
    const headerMenuButton = readComponent('components/navigation/SiteHeaderMenuButton.vue')
    const baseButton = readComponent('components/common/BaseButton.vue')
    const sectionHeading = readComponent('components/common/SectionHeading.vue')
    const sectionHeader = readComponent('components/common/section/SectionHeader.vue')
    const baseCard = readComponent('components/common/card/BaseCard.vue')
    const iconBox = readComponent('components/common/card/IconBox.vue')
    const cardGrid = readComponent('components/common/card/CardGrid.vue')
    const baseTabs = readComponent('components/common/tabs/BaseTabs.vue')
    const productSystemSection = readComponent('components/common/ProductSystemSection.vue')
    const productSystemFlowFrame = readComponent('components/common/ProductSystemFlowFrame.vue')
    const productSystemCards = readComponent('components/common/ProductSystemCards.vue')
    const ctaSection = readComponent('components/common/CtaSection.vue')
    const hero = readComponent('components/home/HomeHero.vue')
    const logos = readComponent('components/home/HomeCustomerLogos.vue')
    const deliverables = readComponent('components/home/HomeDeliverables.vue')
    const productSystem = readComponent('components/home/HomeProductSystem.vue')
    const homeProductSystemFlow = readComponent('components/home/HomeProductSystemFlow.vue')
    const homeProductSystemMobileFlow = readComponent('components/home/HomeProductSystemMobileFlow.vue')
    const homeSolutions = readComponent('components/home/HomeSolutions.vue')
    const homeEcosystem = readComponent('components/home/HomeEcosystem.vue')
    const ecosystemVisual = readComponent('components/home/HomeEcosystemVisual.vue')
    const ecosystemTokenVisual = readComponent('components/home/ecosystem-visuals/EcosystemTokenVisual.vue')
    const ecosystemAgentVisual = readComponent('components/home/ecosystem-visuals/EcosystemAgentVisual.vue')
    const ecosystemInfraVisual = readComponent('components/home/ecosystem-visuals/EcosystemInfraVisual.vue')
    const ecosystemReportVisual = readComponent('components/home/ecosystem-visuals/EcosystemReportVisual.vue')
    const ecosystemVisualData = readComponent('data/ecosystemVisual.ts')
    const homeCases = readComponent('components/home/HomeCases.vue')
    const homeCaseSlide = readComponent('components/home/HomeCaseSlide.vue')
    const carouselRoot = readComponent('components/common/carousel/CarouselRoot.vue')
    const carouselControls = readComponent('components/common/carousel/CarouselControls.vue')
    const homeInsights = readComponent('components/home/HomeInsights.vue')
    const homeCta = readComponent('components/home/HomeCta.vue')
    const siteFooter = readComponent('components/layout/SiteFooter.vue')
    const footerSubscribe = readComponent('components/layout/FooterSubscribe.vue')
    const footerMain = readComponent('components/layout/FooterMain.vue')
    const footerSocials = readComponent('components/layout/FooterSocials.vue')
    const footerBottom = readComponent('components/layout/FooterBottom.vue')
    const footerData = readComponent('data/footer.ts')
    const footerMainIndex = siteFooter.indexOf('<FooterMain')
    const footerSocialsIndex = siteFooter.indexOf('<FooterSocials')
    const footerBottomRuleIndex = siteFooter.indexOf('class="site-footer__rule site-footer__rule--bottom"')
    const footerSources = [siteFooter, footerSubscribe, footerMain, footerSocials, footerBottom, footerData].join('\n')
    const enterpriseFlow = readComponent('components/flow/EnterpriseFlow.client.vue')

    expect(sectionHeading).toContain('SectionHeader')
    expect(sectionHeader).toContain('section-heading--nowrap-subtitle')
    expect(sectionHeader).toContain('max-width: none')
    expect(tailwind).toContain('@import "tailwindcss"')
    expect(tailwind).toContain('@theme inline')
    expect(tailwind).toContain('--color-dt-primary: var(--dt-color-primary)')
    expect(tailwind).toContain('--color-dimmed: var(--dt-color-text-muted)')
    expect(tailwind).toContain('@utility bg-default')
    expect(tailwind).toContain('background-color: var(--dt-color-bg)')
    expect(tailwind).toContain('@utility border-default')
    expect(tokens).toContain('--dt-container: 80rem')
    expect(tokens).toContain('--dt-container-2xl: 96rem')
    expect(tokens).toContain('--dt-color-bg: #151515')
    expect(tokens).toContain('--dt-color-text-highlighted: #ffffff')
    expect(tokens).toContain('--dt-color-nav-text: #e5e5e5')
    expect(tokens).toContain('--dt-radius-lg: 16px')
    expect(tokens).toContain('@media (min-width: 1536px)')
    expect(tokens).toContain('--dt-gradient-spotlight')
    expect(tokens).toContain('--dt-card-radius: var(--dt-radius-lg)')
    expect(tokens).toContain('--dt-icon-box-radius: var(--dt-radius-md)')
    expect(tokens).toContain('.dt-button')
    expect(tokens).toContain('.dt-card')
    expect(tokens).toContain('.dt-card--adaptive')
    expect(tokens).toContain('.dt-card--feature')
    expect(tokens).toContain('.dt-card--soft')
    expect(tokens).toContain('.dt-tab-list')
    expect(tokens).toContain('.dt-tab')
    expect(tokens).toContain('.dt-segmented-tabs')
    expect(tokens).toContain('.dt-segmented-tab')
    expect(tokens).toContain('.dt-product-card')
    expect(tokens).toContain('.dt-ecosystem-card')
    expect(tokens).toContain('.dt-icon-box')
    expect(tokens).toContain('border-radius: var(--dt-icon-box-radius)')
    expect(tokens).toContain('border-radius: var(--dt-card-radius)')
    expect(tokens).toContain('transform: translateY(-4px)')
    expect(tokens).toContain('box-shadow: var(--dt-card-hover-shadow, var(--dt-shadow-primary))')
    expect(tokens).not.toContain('min-height: 280px')
    expect(tokens).toContain('.dt-card-tag')
    expect(tokens).toContain('.dt-cta-panel')
    expect(tokens).not.toContain('@apply')
    expect(baseButton).toContain('data-slot="base"')
    expect(baseButton).toContain('data-slot="label"')
    expect(baseButton).toContain('data-slot="trailingIcon"')
    expect(baseButton).toContain('dt-button')
    expect(sectionHeading).toContain('titleId')
    expect(sectionHeading).toContain('nowrapSubtitle')
    expect(sectionHeader).toContain('align?: \'left\' | \'center\' | \'right\'')
    expect(sectionHeader).toContain('level?: 1 | 2 | 3')
    expect(sectionHeader).toContain('slots.actions')
    expect(sectionHeader).toContain('class="section-heading dt-section-heading"')

    expect(header).toContain('height: var(--dt-header-height)')
    expect(header).toContain('margin-bottom: calc(var(--dt-header-height) * -1)')
    expect(header).toContain('data-slot="root"')
    expect(header).toContain('data-slot="container"')
    expect(header).toContain('src="/logo-while.svg"')
    expect(header).toContain('width: 200px')
    expect(header).not.toContain('filter: brightness(0) invert(1)')
    expect(header).toContain('background: rgba(21, 21, 21, 0.95)')
    expect(header).toContain('0 10px 15px -3px rgba(0, 0, 0, 0.1)')
    expect(header).toContain('site-header__left')
    expect(header).toContain('flex: 1 1 0')
    expect(header).toContain('SiteHeaderDesktopNav')
    expect(header).toContain('SiteHeaderActions')
    expect(header).toContain('SiteHeaderMobileNav')
    expect(header).toContain('SiteHeaderMenuButton')
    expect(header).toContain('function handleNavMouseEnter')
    expect(header).toContain('function handleNavMouseLeave')
    expect(header).not.toContain('max-width: var(--dt-container)')
    expect(header).not.toContain('@media (max-width: 1279px)')
    expect(headerActions).toContain('site-header__github-icon')
    expect(headerActions).toContain('site-header__language-icon')
    expect(headerActions).toContain('site-header__github-icon size-5')
    expect(headerActions).toContain('site-header__language-icon size-5')
    expect(headerActions).toContain('site-header__text-link')
    expect(headerActions).toContain('登录OPS')
    expect(headerActions).toContain('font-size: 14px')
    expect(headerActions).not.toContain('GitFork')
    expect(headerActions).not.toContain('联系我们')
    expect(headerActions).not.toContain('免费咨询')
    expect(headerDesktopNav).toContain('<div style="position:relative;">')
    expect(headerDesktopNav).toContain('@mouseenter="$emit(\'enter\', item, index)"')
    expect(headerDesktopNav).toContain('@mouseleave="$emit(\'leave\', item)"')
    expect(headerDesktopNav).toContain(":class=\"{ 'direct-link': !hasMega(item) }\"")
    expect(headerDesktopNav).toContain('color: var(--dt-color-nav-text)')
    expect(headerDesktopNav).toContain('color: var(--dt-color-text-highlighted)')
    expect(headerDesktopNav).toContain('&.direct-link:hover')
    expect(headerDesktopNav).toContain('color: var(--dt-color-primary)')
    expect(headerDesktopNav).toContain('rgba(39, 30, 53, 0.5)')
    expect(headerDesktopNav).toContain('color-mix(in oklab, var(--dt-color-bg-elevated) 50%, transparent)')
    expect(headerDesktopNav).toContain('border-radius: var(--dt-ui-radius)')
    expect(headerMobileNav).toContain('mobile-navigation')
    expect(headerMenuButton).toContain('Menu')
    expect(headerMenuButton).toContain('X')
    expect(page).toContain('class="home-hero-deliverables"')
    expect(page).toContain('class="home-hero-deliverables__video"')
    expect(page).toContain('class="home-hero-deliverables__mask"')
    expect(page).toContain('rgba(21, 21, 21, 0.4)')
    expect(page).toContain('rgba(21, 21, 21, 0.95) 0%')
    expect(page).toContain('var(--dt-color-bg) 100%')
    expect(page).toContain('      </div>\n      <HomeProductSystem />')
    expect(hero).toContain('padding-top: 128px')
    expect(hero).toContain('font-size: 60px')
    expect(hero).toContain('background: transparent')
    expect(hero).not.toContain('home-hero__wash')
    expect(hero).not.toContain('ClientOnly')
    expect(logos).toContain('padding-top: 5.5rem')
    expect(logos).toContain('gap: 4rem')
    expect(deliverables).toContain('padding: clamp(3.5rem, 7vw, 6rem) 0 8rem')
    expect(deliverables).toContain('flex: 0 0 40%')
    expect(deliverables).toContain('min-height: 280px')
    expect(deliverables).toContain('padding: 48px')
    expect(deliverables).toContain('padding-right: 112px')
    expect(deliverables).toContain('linear-gradient(to right, #181524, #271e35)')
    expect(deliverables).toContain('aria-roledescription="轮播"')
    expect(deliverables).toContain('showPreviousDeliverable')
    expect(deliverables).toContain('showNextDeliverable')
    expect(deliverables).toContain('data-active-slide')
    expect(deliverables).toContain('flex: 0 0 100%')
    expect(deliverables).toContain('transform: translate3d(-100%, 0, 0)')
    expect(deliverables).toContain('transform: translate3d(-200%, 0, 0)')
    expect(deliverables).not.toContain('animation:')
    expect(deliverables).not.toContain('deliverablesCarousel')
    expect(deliverables).not.toContain('deliverablesMarquee')
    expect(deliverables).toContain('font-size: 20px')
    expect(deliverables).toContain('font-size: 24px')
    expect(deliverables).toContain('font-size: 14px')
    expect(deliverables).toContain('display: none')
    expect(deliverables).not.toContain('What We Deliver')

    expect(productSystem).toContain('eyebrow="DeepTrols OPS"')
    expect(productSystem).toContain('title="连接真实业务与人工智能"')
    expect(productSystem).toContain('以数据与知识底座桥接企业业务与人工智能。赋能 AI Agent，实现对真实业务的支撑。')
    expect(productSystem).toContain('ProductSystemSection')
    expect(productSystem).toContain('HomeProductSystemFlow')
    expect(productSystem).toContain('HomeProductSystemMobileFlow')
    expect(productSystem).toContain('ProductSystemCards')
    expect(homeProductSystemFlow).toContain('ProductSystemFlowFrame')
    expect(homeProductSystemFlow).toContain('shouldRenderFlow')
    expect(homeProductSystemFlow).toContain('<EnterpriseFlow v-if="shouldRenderFlow" />')
    expect(productSystemSection).toContain('SectionShell')
    expect(productSystemSection).toContain('SectionHeader')
    expect(productSystemSection).toContain('nowrap-subtitle')
    expect(productSystemSection).toContain('subtitle-size="large"')
    expect(productSystemSection).toContain(":width=\"headingWide ? 'wide' : 'default'\"")
    expect(sectionHeader).toContain('section-heading--subtitle-large')
    expect(sectionHeader).toContain('font-size: 20px')
    expect(sectionHeader).toContain('white-space: nowrap')
    expect(productSystemSection).toContain('product-system__content')
    expect(productSystemSection).not.toContain('role="img"')
    expect(productSystemSection).not.toContain('product-system__mobile-flow')
    expect(productSystemSection).not.toContain('v-for="card in cards"')
    expect(productSystemSection).not.toContain('flowFallbackText')
    expect(productSystemSection).not.toContain('EnterpriseFlow')
    expect(productSystem).toContain('label="DeepTrols OPS platform architecture"')
    expect(productSystemFlowFrame).toContain('role="img"')
    expect(productSystemFlowFrame).toContain('height: 560px')
    expect(productSystemFlowFrame).toContain('rgba(148, 163, 184, 0.12) 1px')
    expect(productSystemFlowFrame).toContain('background-size: 48px 48px')
    expect(homeProductSystemMobileFlow).toContain('product-system__mobile-flow')
    expect(homeProductSystemMobileFlow).toContain('inputs')
    expect(homeProductSystemMobileFlow).toContain('outputs')
    expect(productSystemCards).toContain('CardGrid')
    expect(productSystemCards).toContain('BaseCard')
    expect(productSystemCards).toContain('IconBox')
    expect(productSystemCards).toContain('columns="four" gap="sm"')
    expect(productSystemCards).toContain('variant="product"')
    expect(productSystemCards).toContain('accent equal-height')
    expect(baseCard).toContain('dt-product-card')
    expect(baseCard).toContain('dt-card--adaptive')
    expect(baseCard).toContain('dt-product-card__accent')
    expect(iconBox).toContain('dt-icon-box')
    expect(cardGrid).toContain('md:grid-cols-2 lg:grid-cols-4')
    expect(productSystemCards).not.toContain('min-height')
    expect(productSystemCards).toContain('<h3>{{ card.name }}</h3>')
    expect(productSystemCards).not.toContain('{{ card.title }}')

    expect(homeSolutions).toContain('eyebrow="Use Cases"')
    expect(homeSolutions).toContain('title="驱动各行业智能提升"')
    expect(homeSolutions).toContain('覆盖智能制造、企业运营、AI基础设施等核心领域，帮助企业快速构建可持续演进的智能化能力')
    expect(homeSolutions).toContain('nowrap-subtitle')
    expect(homeSolutions).toContain('.solutions :deep(.section-heading__subtitle)')
    expect(homeSolutions).toContain('font-size: 20px')
    expect(homeSolutions).toContain('white-space: nowrap')
    expect(homeSolutions).toContain('BaseTabs')
    expect(homeSolutions).toContain('solutionTabs')
    expect(homeSolutions).toContain('label="解决方案分类"')
    expect(homeSolutions).toContain('variant="pill"')
    expect(baseTabs).toContain('role="tablist"')
    expect(baseTabs).toContain('class="base-tabs"')
    expect(baseTabs).toContain('dt-tab-list')
    expect(baseTabs).toContain('dt-tab')
    expect(homeSolutions).toContain('CarouselRoot')
    expect(homeSolutions).toContain(':active-index="activeIndex"')
    expect(homeSolutions).toContain(':item-count="solutions.length"')
    expect(homeSolutions).toContain('labelled-by="solutions-title"')
    expect(homeSolutions).toContain('--dt-carousel-align: flex-start')
    expect(homeSolutions).toContain('--dt-carousel-gutter: -16px')
    expect(homeSolutions).not.toContain('solutions__carousel-container')
    expect(carouselRoot).toContain('role="region"')
    expect(carouselRoot).toContain('aria-roledescription="carousel"')
    expect(carouselRoot).toContain('data-orientation="horizontal"')
    expect(carouselRoot).toContain('data-slot="root"')
    expect(carouselRoot).toContain('data-slot="viewport"')
    expect(carouselRoot).toContain('data-slot="container"')
    expect(carouselRoot).toContain('transform: translate3d(-500%, 0, 0)')
    expect(carouselRoot).toContain('align-items: var(--dt-carousel-align, stretch)')
    expect(carouselRoot).toContain('margin-inline-start: var(--dt-carousel-gutter, 0px)')
    expect(homeSolutions).toContain('role="group"')
    expect(homeSolutions).toContain('data-slot="item"')
    expect(homeSolutions).toContain('flex: 0 0 100%')
    expect(homeSolutions).toContain('min-height: 420px')
    expect(homeSolutions).toContain('min-height: 520px')
    expect(homeSolutions).toContain('background: linear-gradient(to top, rgba(0, 0, 0, 0.95)')
    expect(homeSolutions).toContain('padding: 96px 24px 24px')
    expect(homeSolutions).toContain('font-size: 24px')
    expect(homeSolutions).toContain('class="group solutions__panel"')
    expect(homeSolutions).toContain('阅读案例')
    expect(homeSolutions).toContain('class="iconify iconify--lucide size-4 transition group-hover:translate-x-1"')
    expect(homeSolutions).toContain('line-height: 1.5')
    expect(homeSolutions).not.toContain('了解更多')
    expect(homeSolutions).toContain('查看所有应用场景')
    expect(homeSolutions).toContain('href="/solutions#use-cases"')
    expect(homeSolutions).toContain('BaseButton')
    expect(homeSolutions).not.toContain('查看所有解决方案')

    expect(homeEcosystem).toContain('eyebrow="ecosystem"')
    expect(homeEcosystem).toContain('title="连接企业 AI 全链路的开放生态"')
    expect(homeEcosystem).toContain('subtitle="连接算力、模型、社区与行业知识，构建开放 AI 生态"')
    expect(homeEcosystem).toContain('nowrap-subtitle')
    expect(homeEcosystem).toContain('.ecosystem :deep(.section-heading__subtitle)')
    expect(homeEcosystem).toContain('font-size: 20px')
    expect(homeEcosystem).toContain('white-space: nowrap')
    expect(homeEcosystem).toContain('HomeEcosystemVisual')
    expect(homeEcosystem).toContain('class="group ecosystem-card dt-ecosystem-card"')
    expect(homeEcosystem).toContain('class="ecosystem-card__icon-box dt-icon-box"')
    expect(homeEcosystem).toContain('class="ecosystem-card__tag dt-card-tag"')
    expect(homeEcosystem).not.toContain('border-radius: var(--dt-radius-md)')
    expect(homeEcosystem).toContain(':to="card.href"')
    expect(homeEcosystem).toContain('grid-template-columns: 1fr')
    expect(homeEcosystem).toContain('grid-template-columns: repeat(2, minmax(0, 1fr))')
    expect(homeEcosystem).toContain('gap: 24px')
    expect(homeEcosystem).toContain('margin-top: 48px')
    expect(homeEcosystem).toContain('width: 48px')
    expect(homeEcosystem).toContain('height: 48px')
    expect(homeEcosystem).toContain('font-size: 20px')
    expect(homeEcosystem).toContain('max-width: 75%')
    expect(homeEcosystem).toContain('font-size: 16px')
    expect(homeEcosystem).toContain('width: 32px')
    expect(homeEcosystem).toContain('height: 32px')
    expect(homeEcosystem).toContain('bottom: 32px')
    expect(homeEcosystem).toContain('opacity: 0')
    expect(homeEcosystem).toContain(':deep(.ecosystem-visual__cloud-grid)')
    expect(homeEcosystem).toContain(':deep(.ecosystem-visual__base-line)')
    expect(homeEcosystem).toContain(':deep(.ecosystem-visual__flow)')
    expect(homeEcosystem).toContain(':deep(.ecosystem-visual__led)')
    expect(homeEcosystem).toContain(':deep(.ecosystem-visual__device-label)')
    expect(homeEcosystem).not.toContain('title="Ecosystem"')
    expect(ecosystemVisual).toContain("type EcosystemVisualVariant = 'token' | 'agent' | 'infra' | 'report'")
    expect(ecosystemVisual).toContain('ecosystem-visual--${variant}')
    expect(ecosystemVisual).toContain('visualComponents[variant]')
    expect(ecosystemVisual).toContain('EcosystemTokenVisual')
    expect(ecosystemVisual).toContain('EcosystemAgentVisual')
    expect(ecosystemVisual).toContain('EcosystemInfraVisual')
    expect(ecosystemVisual).toContain('EcosystemReportVisual')
    expect(ecosystemTokenVisual).toContain('cloudDots')
    expect(ecosystemTokenVisual).toContain('r="0.35"')
    expect(ecosystemTokenVisual).toContain('filter id="glow-map"')
    expect(ecosystemAgentVisual).toContain('filter id="edgeGlow"')
    expect(ecosystemInfraVisual).toContain('filter id="serverGlow"')
    expect(ecosystemReportVisual).toContain('filter id="bridgeGlow"')
    expect(ecosystemVisualData).toContain('x1: 105')
    expect(ecosystemVisualData).toContain('x2: 125')
    expect(ecosystemVisualData).toContain('x1: 120')
    expect(ecosystemVisualData).toContain('x2: 140')
    expect(ecosystemVisualData).toContain('x1: 80')
    expect(ecosystemVisualData).toContain('x2: 115')
    expect(ecosystemVisualData).toContain('transform: \'translate(115, 21)\'')
    expect(ecosystemVisualData).toContain('transform: \'translate(135, 45)\'')
    expect(ecosystemVisualData).toContain('transform: \'translate(105, 58)\'')
    expect(ecosystemReportVisual).toContain('论文')
    expect(ecosystemReportVisual).toContain('领域')
    expect(ecosystemVisual).toContain('top: -5px')
    expect(ecosystemVisual).toContain('right: -5%')
    expect(ecosystemVisual).toContain('top: -5%')
    expect(ecosystemVisual).toContain('height: 110%')
    expect(ecosystemVisual).toContain('width: 80%')
    expect(ecosystemVisual).toContain('width: 65%')
    expect(ecosystemAgentVisual).toContain('stroke-dasharray="1 2"')
    expect(ecosystemAgentVisual).toContain('stroke-dasharray="1 15"')
    expect(ecosystemVisual).toContain('animation-play-state: paused')
    expect(ecosystemVisual).toContain('stroke-dashoffset: 16')
    expect(ecosystemVisual).toContain('@keyframes ecosystemVisualFlow')
    expect(ecosystemVisual).toContain('@keyframes ecosystemVisualPulse')

    expect(homeCases).toContain('CUSTOMER STORIES')
    expect(homeCases).toContain('携手 DeepTrols 加速创新')
    expect(homeCases).toContain('助力客户加速创新、实现业务突破，同时显著降低成本。')
    expect(homeCases).toContain('SectionHeading')
    expect(homeCases).toContain('HomeCaseSlide')
    expect(homeCases).toContain('CarouselRoot')
    expect(homeCases).toContain('CarouselControls')
    expect(homeCases).toContain('title-id="cases-title"')
    expect(homeCases).toContain('nowrap-subtitle')
    expect(homeCases).toContain('max-width: 96rem')
    expect(homeCases).toContain('padding: 0 16px 128px')
    expect(homeCases).toContain('padding-bottom: 176px')
    expect(homeCases).toContain('font-size: 20px')
    expect(homeCases).toContain('white-space: nowrap')
    expect(homeCases).toContain('padding: 40px 0 0')
    expect(homeCases).toContain('cases__shell')
    expect(homeCases).toContain('overflow: hidden')
    expect(homeCases).toContain('border-radius: 24px')
    expect(homeCases).toContain('background: rgba(38, 38, 38, 0.4)')
    expect(homeCases).toContain('0 1px 2px rgba(0, 0, 0, 0.05)')
    expect(homeCases).toContain('cases__controls')
    expect(homeCases).toContain('previous-label="Previous story"')
    expect(homeCases).toContain('next-label="Next story"')
    expect(carouselControls).toContain('previousLabel')
    expect(carouselControls).toContain('nextLabel')
    expect(carouselControls).toContain('dt-icon-button')
    expect(homeCases).toContain('role="tablist"')
    expect(homeCases).toContain('cases__logo-tabs')
    expect(homeCases).toContain('gap: 40px')
    expect(homeCases).toContain('gap: 56px')
    expect(homeCases).toContain(':active-index="activeStoryIndex"')
    expect(homeCases).toContain(':item-count="storyCount"')
    expect(homeCases).toContain(':label="activeStory?.title"')
    expect(homeCases).not.toContain('cases__track')
    expect(carouselRoot).toContain('data-active-slide')
    expect(carouselRoot).toContain('transform: translate3d(-100%, 0, 0)')
    expect(carouselRoot).toContain('transform: translate3d(-200%, 0, 0)')
    expect(homeCaseSlide).toContain('min-height: 520px')
    expect(homeCaseSlide).toContain('<div class="cases__visual-overlay"></div>')
    expect(homeCaseSlide).not.toContain('<span></span>')
    expect(homeCaseSlide).not.toContain('span:nth-child')
    expect(homeCaseSlide).toContain('class="cases__button"')
    expect(homeCaseSlide).toContain(':href="story.href"')
    expect(homeCaseSlide).toContain('margin-top: auto')
    expect(homeCaseSlide).toContain('padding-top: 32px')
    expect(homeCaseSlide).toContain('BaseButton')
    expect(homeCaseSlide).toContain('variant="secondary"')
    expect(homeCaseSlide).toContain('阅读案例')
    expect(homeCases).toContain('class="cases__all-action"')
    expect(homeCases).toContain('href="/cases"')
    expect(homeCases).toContain('查看所有案例')
    expect(homeCases).toContain('margin-top: 32px')
    expect(homeCaseSlide).not.toContain('type="button"\n                          data-slot="base"')
    expect(homeCaseSlide).not.toContain('class="cases__read-link"')
    expect(homeCases).not.toContain('function readStory')
    expect(homeCases).not.toContain('navigateTo')
    expect(homeCaseSlide).not.toContain('阅读案例研究')
    expect(homeCaseSlide).not.toContain('class="cases__link"')
    expect(homeCaseSlide).toMatch(/h3 \{[\s\S]*font-size: 20px;[\s\S]*font-weight: 600;[\s\S]*line-height: 1\.25;/)
    expect(homeCaseSlide).toMatch(/\.cases__content p \{[\s\S]*font-size: 18px;[\s\S]*line-height: 1\.625;/)
    expect(homeCaseSlide).toContain('h3 {\n    font-size: 24px;\n  }')
    expect(homeCaseSlide).toContain('.cases__content p {\n    font-size: 20px;\n  }')
    expect(homeCases).not.toContain('title="CUSTOMER STORIES"')

    expect(homeInsights).toContain('SectionHeading')
    expect(homeInsights).toContain('eyebrow="Resources"')
    expect(homeInsights).toContain('title="创新、洞察与新闻"')
    expect(homeInsights).toContain('title-id="insights-title"')
    expect(homeInsights).toContain('class="insights__layout"')
    expect(homeInsights).toContain('grid-template-columns: 1fr')
    expect(homeInsights).toContain('grid-template-columns: repeat(2, minmax(0, 1fr))')
    expect(homeInsights).toContain('gap: 24px')
    expect(homeInsights).toContain('margin-bottom: 48px')
    expect(homeInsights).toContain('font-size: 16px')
    expect(homeInsights).toContain('class="group insights__featured"')
    expect(homeInsights).toContain('class="group insights__item"')
    expect(homeInsights).toContain('aspect-ratio: 2 / 1')
    expect(homeInsights).toContain('aspect-ratio: 400 / 180')
    expect(homeInsights).toContain('aspect-ratio: auto')
    expect(homeInsights).toContain('border-radius: var(--dt-ui-radius)')
    expect(homeInsights).toContain('border-color: var(--dt-color-line-strong)')
    expect(homeInsights).toContain('transform: scale(1.05)')
    expect(homeInsights).toContain('transition: transform 500ms ease')
    expect(homeInsights).toContain('position: absolute')
    expect(homeInsights).toContain('inset: 0')
    expect(homeInsights).toContain('object-fit: cover')
    expect(homeInsights).toContain('-webkit-line-clamp: 3')
    expect(homeInsights).toContain('-webkit-line-clamp: 2')
    expect(homeInsights).toContain('-webkit-line-clamp: 1')
    expect(homeInsights).toContain('class="insights__all-link insights__all-link--desktop"')
    expect(homeInsights).toContain('class="insights__mobile-action"')
    expect(homeInsights).toContain('BaseButton')
    expect(homeInsights).toContain('查看全部资源')
    expect(homeInsights).not.toContain('insights__topline')
    expect(homeInsights).not.toContain('transform: translateY')

    expect(homeCta).toContain('CtaSection')
    expect(ctaSection).toContain('class="cta-section"')
    expect(ctaSection).toContain('class="container"')
    expect(ctaSection).toContain('class="cta-section__panel dt-cta-panel"')
    expect(homeCta).toContain('构建企业自身的AI能力')
    expect(homeCta).toContain('让数据成为资产，让知识驱动决策，让AI创造价值')
    expect(ctaSection).toContain('padding-bottom: 128px')
    expect(ctaSection).toContain('padding-bottom: 176px')
    expect(ctaSection).toContain('font-size: 36px')
    expect(ctaSection).toContain('font-size: 48px')
    expect(ctaSection).toContain('font-size: 20px')
    expect(ctaSection).toContain('class="cta-section__actions"')
    expect(ctaSection).toContain('gap: 24px')
    expect(ctaSection).toContain('BaseButton')
    expect(homeCta).toContain("href: '/contact'")
    expect(homeCta).toContain("href: '/try'")
    expect(homeCta).toContain('咨询合作')
    expect(homeCta).toContain('申请试用')
    expect(homeCta).not.toContain('Start With DeepTrols')
    expect(homeCta).not.toContain('构建属于企业自己的 AI 能力')

    expect(siteFooter).toContain('class="site-footer"')
    expect(siteFooter).toContain('padding-top: 48px')
    expect(siteFooter).toContain('background: var(--dt-color-bg-soft)')
    expect(siteFooter).toContain('FooterSubscribe')
    expect(siteFooter).toContain('FooterMain')
    expect(siteFooter).toContain('FooterSocials')
    expect(siteFooter).toContain('FooterBottom')
    expect(footerSubscribe).toContain('订阅 DeepTrols 最新资讯')
    expect(footerSubscribe).toContain('data-slot="base"')
    expect(footerSubscribe).toContain('dt-button dt-button--primary dt-button--lg')
    expect(footerMain).toContain('class="site-footer__main"')
    expect(footerMain).toContain('class="site-footer__identity"')
    expect(footerMain).toContain('src="/logo-while.svg"')
    expect(footerMain).toContain('width: 240px')
    expect(footerMainIndex).toBeGreaterThan(-1)
    expect(footerSocialsIndex).toBeGreaterThan(footerMainIndex)
    expect(footerSocialsIndex).toBeLessThan(footerBottomRuleIndex)
    expect(footerSocials).toContain('class="site-footer__socials"')
    expect(footerSocials).toContain('aria-label="社交媒体"')
    expect(footerSocials).toContain('class="site-footer__social-link"')
    expect(footerData).toContain("label: 'GitHub'")
    expect(footerData).toContain("label: 'X'")
    expect(footerData).toContain("label: '哔哩哔哩'")
    expect(footerData).toContain("label: 'LinkedIn'")
    expect(footerData).toContain("label: '微信公众号'")
    expect(footerSocials).toContain('margin-top: 40px')
    expect(footerSocials).toContain('gap: 24px')
    expect(footerSocials).toContain('iconify iconify--simple-icons w-full h-6')
    expect(footerMain).toContain('class="site-footer__columns"')
    expect(footerMain).toContain('grid-template-columns: repeat(5, minmax(0, 1fr))')
    expect(footerMain).toContain('gap: 32px')
    expect(footerMain).toContain('font-size: 14px')
    expect(footerMain).toContain('line-height: 20px')
    expect(footerMain).toContain('gap: 10px')
    expect(footerMain).toContain('margin: 16px 0')
    expect(footerMain).toContain('class="site-footer__group-rule"')
    expect(footerData).toContain('产品')
    expect(footerData).toContain('数曜·数据治理平台')
    expect(footerData).toContain('数曜·数据标签平台')
    expect(footerData).toContain('数曜·数据开发平台')
    expect(footerData).toContain('数曜·数据要素监管平台')
    expect(footerData).not.toContain('数曜·数据资产管理平台')
    expect(footerData).toContain('博曜·企业级知识管理平台')
    expect(footerData).toContain('探曜·AI物联感知平台')
    expect(footerData).toContain('Device Agent')
    expect(footerData).toContain('API安全管控平台')
    expect(footerData).toContain('API安全审计系统')
    expect(footerData).toContain('业务威胁感知系统')
    expect(footerData).toContain('使用场景')
    expect(footerData).toContain('智能制造')
    expect(footerData).toContain('智能环保')
    expect(footerData).toContain('智能水利')
    expect(footerData).toContain('智慧能源')
    expect(footerData).toContain('数据治理')
    expect(footerData).toContain('企业AI应用')
    expect(footerData).toContain('智慧城市')
    expect(footerData).toContain('智慧医疗')
    expect(footerData).toContain('行业')
    expect(footerData).toContain('制造业')
    expect(footerData).toContain('能源')
    expect(footerData).toContain('交通运输与物流')
    expect(footerData).toContain('政府/公共事业')
    expect(footerData).toContain('教育')
    expect(footerData).toContain('水利/水务')
    expect(footerData).toContain('金融服务')
    expect(footerData).toContain('城市治理')
    expect(footerData).toContain('零售')
    expect(footerData).toContain('服务')
    expect(footerData).toContain('Token Hub')
    expect(footerData).toContain('白皮书&报告')
    expect(footerData).toContain('产品文档')
    expect(footerData).toContain('算力与基础设施')
    expect(footerData).toContain('智能体社区')
    expect(footerMain).toContain('translate-inline-px translate-y-[-2px] scale-inline-100 transform')
    expect(footerMain).toContain('stroke-width="1.3"')
    expect(footerData).toContain('公司')
    expect(footerData).toContain('关于我们')
    expect(footerData).toContain('新闻动态')
    expect(footerData).toContain('合作伙伴')
    expect(footerData).toContain('加入我们')
    expect(footerData).toContain('联系我们')
    expect(footerData).toContain('品牌资源')
    expect(footerData).toContain('服务案例')
    expect(footerData).toContain('投资计划')
    expect(footerBottom).toContain('class="site-footer__filings"')
    expect(footerBottom).toContain('京公网安备100861001010000号')
    expect(footerBottom).toContain('京ICP备20018888号-4')
    expect(footerBottom).toContain('https://beian.miit.gov.cn/')
    expect(footerBottom).toContain('class="site-footer__copyright"')
    expect(footerBottom).toContain('@ 武汉深度数智科技有限公司 2026 版权所有')
    expect(footerBottom).toContain('justify-content: space-between')
    expect(footerSources).not.toContain('© 2026 DeepTrols. All rights reserved')
    expect(footerSources).not.toContain('legalLinks')
    expect(footerSources).not.toContain('class="site-footer__locale"')
    expect(footerSources).not.toContain('site-footer__locale-leading')
    expect(footerSources).not.toContain('site-footer__locale-trailing')
    expect(footerSources).not.toContain('aria-label="切换语言"')
    expect(footerSources).not.toContain('primaryNavigation')
    expect(footerSources).not.toContain('flattenColumnLinks')
    expect(footerSources).not.toContain('Mail')
    expect(footerSources).not.toContain('MapPin')
    expect(footerSources).not.toContain('Phone')
    expect(enterpriseFlow).toContain('height: 100%')
    expect(enterpriseFlow).toContain('background: transparent')
    expect(enterpriseFlow).not.toContain('border: 1px solid var(--dt-color-line)')
  })

  it('exposes configurable icon box size, border, and fill through the feature grid', () => {
    const iconBox = readComponent('components/common/card/IconBox.vue')
    const featureCard = readComponent('components/common/card/FeatureCard.vue')
    const featureGrid = readComponent('components/common/ProductFeatureGridSection.vue')

    expect(iconBox).toContain('dt-icon-box')
    expect(iconBox).toContain('dt-icon-box--gradient')
    expect(iconBox).toContain('bordered?: boolean')
    expect(iconBox).toContain('filled?: boolean')
    expect(iconBox).toContain('bordered: true')
    expect(iconBox).toContain('filled: true')
    expect(iconBox).toContain('icon-box--borderless')
    expect(iconBox).toContain('icon-box--unfilled')
    expect(iconBox).toContain('--dt-icon-box-shadow: none')
    expect(iconBox).toContain('--dt-icon-box-bg: transparent')
    expect(featureCard).toContain('iconBordered?: boolean')
    expect(featureCard).toContain('iconFilled?: boolean')
    expect(featureCard).toContain(':bordered="iconBordered"')
    expect(featureCard).toContain(':filled="iconFilled"')
    expect(featureGrid).toContain('iconBoxSize?: 40 | 48 | 56')
    expect(featureGrid).toContain('iconSize?: 20 | 24 | 28 | 48')
    expect(featureGrid).toContain('iconBordered?: boolean')
    expect(featureGrid).toContain('iconFilled?: boolean')
    expect(featureGrid).toContain(':icon-size="iconSize"')
    expect(featureGrid).toContain(':icon-box-size="iconBoxSize"')
    expect(featureGrid).toContain(':icon-bordered="iconBordered"')
    expect(featureGrid).toContain(':icon-filled="iconFilled"')
  })

  it('renders the boyao hero card icon boxes through the shared IconBox soft tone', () => {
    const iconBox = readComponent('components/common/card/IconBox.vue')
    const featureCard = readComponent('components/common/card/FeatureCard.vue')
    const heroVisual = readComponent('components/product/boyao/BoyaoHeroVisual.vue')

    expect(iconBox).toContain("tone?: 'primary' | 'muted' | 'white' | 'gradient' | 'soft'")
    expect(iconBox).toContain('icon-box--tone-soft')
    expect(iconBox).toContain('color-mix(in oklab, var(--dt-color-bg-soft) 50%, transparent)')
    expect(featureCard).toContain("tone?: 'primary' | 'muted' | 'white' | 'gradient' | 'soft'")
    expect(heroVisual).toContain('IconBox')
    expect(heroVisual).toContain(':size="40" tone="soft"')
    expect(heroVisual).toContain(':size="56"')
    expect(heroVisual).toContain('class="col-span-2"')
    expect(heroVisual).toContain('capabilityStyles[index]?.iconBox')
    expect(heroVisual).not.toContain('flex h-10 w-10 items-center justify-center rounded-[16px]')
    expect(heroVisual).not.toContain('flex h-14 items-center justify-center rounded-[12px]')
    expect(heroVisual).toContain('hidden w-full lg:block')
    expect(heroVisual).toContain('pointer-events-none relative ml-auto h-[520px] w-full max-w-[680px]')
    expect(heroVisual).not.toContain('max-w-2xl')
  })

  it('renders the boyao core capability section through the shared AlternatingTimelineSection', () => {
    const capability = readComponent('components/product/boyao/BoyaoCapabilitySection.vue')
    const page = readComponent('pages/products/knowledge-base.vue')
    const boyaoData = readComponent('data/boyao.ts')

    expect(capability).toContain('AlternatingTimelineSection')
    expect(capability).toContain('boyaoTimelineItems')
    expect(capability).toContain('eyebrow="核心能力"')
    expect(capability).toContain('title="让企业知识真正理解业务"')
    expect(capability).toContain('title-id="boyao-capability-title"')
    expect(capability).not.toContain('ProductSystemSection')
    expect(boyaoData).toContain('export const boyaoTimelineItems: AlternatingTimelineItem[]')
    expect(boyaoData).not.toContain('boyaoCapabilitiesPlaceholder')
    expect(boyaoData).not.toContain('boyaoShowcases')
    expect(page).toContain('BoyaoCapabilitySection')
    expect(page).not.toContain('ServiceShowcaseSection')
    expect(page).not.toContain('boyaoShowcases')
  })

  it('renders the boyao xinchuang compatibility grid through the shared CompatibilityGridSection', () => {
    const compatibility = readComponent('components/common/CompatibilityGridSection.vue')
    const page = readComponent('pages/products/knowledge-base.vue')
    const boyaoData = readComponent('data/boyao.ts')

    expect(compatibility).toContain('SectionHeader')
    expect(compatibility).toContain('CardGrid')
    expect(compatibility).toContain('columns="three"')
    expect(compatibility).toContain('IconBox')
    expect(compatibility).toContain('export interface CompatibilityGridCategory')
    expect(compatibility).toContain('uppercase tracking-wider')
    expect(page).toContain('CompatibilityGridSection')
    expect(page).toContain('eyebrow="国产化适配"')
    expect(page).toContain('title="适配信创生态"')
    expect(page).toContain('boyaoCompatibilityCategories')
    expect(page.indexOf('<BoyaoIntegrationSection')).toBeLessThan(page.indexOf('<CompatibilityGridSection'))
    expect(boyaoData).toContain('export const boyaoCompatibilityCategories: CompatibilityGridCategory[]')
  })
})

describe('tanyao ai-iot page contract', () => {
  it('renders the tanyao hero through PageHero with a flush-bottom rhythm and the boyao-aligned visual', () => {
    const hero = readComponent('components/product/tanyao/TanyaoHero.vue')
    const pageHero = readComponent('components/common/PageHero.vue')

    expect(hero).toContain('badge="探曜·AI物联感知平台"')
    expect(hero).toContain('RadioTower')
    expect(hero).toContain('title-id="tanyao-hero-title"')
    expect(hero).toContain('title-line="连接、感知、智能"')
    expect(hero).toContain('title-gradient="AIoT智能物联底座"')
    expect(hero).toContain('visual-label="TANYAOIOT_HORE_WEBM"')
    expect(hero).toContain('visual-size="large"')
    expect(hero).toContain('flush-bottom')
    // the visual right edge follows the boyao baseline (ml-auto canvas), not flush-visual-end
    expect(hero).not.toContain('flush-visual-end')
    expect(hero).toContain('<TanyaoHeroVisual />')
    expect(hero).not.toContain('HeroStatsStrip')
    expect(pageHero).toContain('flushVisualEnd?: boolean')
    expect(pageHero).toContain("flushVisualEnd ? 'lg:-mr-4 lg:justify-self-end' : ''")
  })

  it('renders the 1x4 stats strip as a standalone section below the hero', () => {
    const stats = readComponent('components/product/tanyao/TanyaoStatsSection.vue')
    const heroStatsStrip = readComponent('components/common/HeroStatsStrip.vue')

    expect(stats).toContain('HeroStatsStrip')
    expect(stats).toContain('tanyaoHeroStats')
    expect(stats).toContain(':columns="4"')
    expect(stats).toContain('placement="section"')
    expect(stats).toContain('container')
    expect(stats).not.toContain('<style')
    expect(heroStatsStrip).toContain('columns?: 3 | 4')
    expect(heroStatsStrip).toContain('grid-cols-2 sm:grid-cols-4')
    expect(heroStatsStrip).toContain('grid-cols-3')
    expect(heroStatsStrip).toContain("placement?: 'hero' | 'section'")
    expect(heroStatsStrip).toContain('mx-auto mt-8 max-w-2xl lg:mx-0')
    expect(heroStatsStrip).toContain('rounded-xl border border-default bg-dt-bg-soft/50 px-4 py-4 text-center backdrop-blur-sm')
    expect(heroStatsStrip).toContain('text-2xl font-semibold text-highlighted lg:text-3xl')
    expect(heroStatsStrip).toContain('mt-1 text-xs text-muted lg:text-sm')
    expect(heroStatsStrip).toContain('bg-[var(--dt-color-bg-soft)]/50 px-3 py-3 text-center backdrop-blur-sm')
  })

  it('replicates the EMQX Edge hero visual structure with tanyao copy and unique SVG ids', () => {
    const heroVisual = readComponent('components/product/tanyao/TanyaoHeroVisual.vue')

    // Tailwind-only component
    expect(heroVisual).not.toContain('<style')
    expect(heroVisual).not.toMatch(/\sstyle=/)

    // canvas and glow baseline; the root and canvas follow the boyao right-flush geometry
    expect(heroVisual).toContain('class="hidden w-full lg:block"')
    expect(heroVisual).toContain('pointer-events-none relative ml-auto h-[600px] w-full max-w-[600px]')
    expect(heroVisual).toContain('viewBox="0 0 560 560"')
    expect(heroVisual).toContain('id="tanyaoFlowGradientUp"')
    expect(heroVisual).toContain('id="tanyaoFlowGradientDown"')
    expect(heroVisual).toContain('id="tanyaoEdgeGlow"')
    expect(heroVisual).toContain('stroke-dasharray="6 10"')
    expect(heroVisual).toContain('attributeName="stroke-dashoffset"')

    // card radii follow the boyao hero visual baseline (24px cards, 32px platform card)
    expect(heroVisual).toContain('rounded-[24px]')
    expect(heroVisual).toContain('rounded-[32px]')
    expect(heroVisual).not.toContain('rounded-xl ')
    expect(heroVisual).not.toContain('rounded-2xl ')
    expect(heroVisual).not.toContain('rounded-lg ')

    // top platform card
    expect(heroVisual).toContain('absolute left-1/2 top-[18px] w-[460px] -translate-x-1/2')
    expect(heroVisual).toContain('探曜AIoT')
    expect(heroVisual).toContain('智能物联底座')
    expect(heroVisual).toContain('AI Powered')
    expect(heroVisual).toContain('链接生态')
    expect(heroVisual).toContain('tanyaoIotLogo')

    // ecosystem logos keep the AI partners ahead of the 曜 platforms
    expect(heroVisual).toContain('deepseekLogo')
    expect(heroVisual).toContain('qwenLogo')
    expect(heroVisual).toContain('zhipuLogo')
    expect(heroVisual).toContain('kimiLogo')
    expect(heroVisual).toContain('DeepSeek')
    expect(heroVisual).toContain('千问')
    expect(heroVisual).toContain('智谱')
    expect(heroVisual).toContain('Kimi')
    expect(heroVisual.indexOf('DeepSeek')).toBeLessThan(heroVisual.indexOf('数曜'))

    // middle edge card grows to five runtime nodes and stays inside the lg canvas
    expect(heroVisual).toContain('absolute left-1/2 top-[208px] w-[504px] xl:w-[560px] -translate-x-1/2')
    expect(heroVisual).toContain('探曜 Edge')
    expect(heroVisual).toContain('relative grid grid-cols-5 gap-2')
    expect(heroVisual).toContain('设备接入')
    expect(heroVisual).toContain('边缘计算')
    expect(heroVisual).toContain('策略执行')
    expect(heroVisual).toContain('Node-RED')
    expect(heroVisual).toContain('Neuron')
    expect(heroVisual).toContain('animate-pulse-bar')
    expect(heroVisual).toContain('animate-pulse-dot')
    // node padding and broker bars relax at lg so five nodes fit the 520px lg canvas
    expect(heroVisual).toContain('p-3 shadow-sm backdrop-blur-xl transition-all duration-300 hover:-translate-y-0.5 hover:shadow-lg xl:p-4')
    expect(heroVisual).toContain('h-1.5 w-3 animate-pulse-bar rounded-full bg-fuchsia-500/60 xl:w-3.5')

    // bottom device card grows to five tiles
    expect(heroVisual).toContain('absolute bottom-[46px] left-1/2 w-[460px] -translate-x-1/2')
    expect(heroVisual).toContain('grid grid-cols-5 gap-3')
    expect(heroVisual).toContain('工业设备')
    expect(heroVisual).toContain('传感器')
    expect(heroVisual).toContain('能源设备')
    expect(heroVisual).toContain('PLC')
    expect(heroVisual).toContain('摄像头')

    // logo boxes reuse the shared IconBox soft tone
    expect(heroVisual).toContain('IconBox')
    expect(heroVisual).toContain(':size="48" tone="soft"')
    expect(heroVisual).toContain(':size="40" tone="soft"')
  })

  it('assembles the tanyao page from shared section components in the required order', () => {
    const page = readComponent('pages/products/ai-iot.vue')
    const solution = readComponent('components/product/tanyao/TanyaoSolutionSection.vue')
    const capability = readComponent('components/product/tanyao/TanyaoCapabilitySection.vue')
    const timeline = readComponent('components/common/AlternatingTimelineSection.vue')
    const tanyaoData = readComponent('data/tanyao.ts')

    expect(page).toContain('<TanyaoHero />')
    expect(page).toContain('<TanyaoStatsSection />')
    expect(page).toContain('title-id="tanyao-challenge-title"')
    expect(page).toContain('title-id="tanyao-agent-title"')
    expect(page).toContain('title-id="tanyao-cta-title"')
    expect(page.indexOf('<TanyaoHero')).toBeLessThan(page.indexOf('<TanyaoStatsSection'))
    expect(page.indexOf('<TanyaoStatsSection')).toBeLessThan(page.indexOf('tanyao-challenge-title'))
    expect(page.indexOf('tanyao-challenge-title')).toBeLessThan(page.indexOf('<TanyaoSolutionSection'))
    expect(page.indexOf('<TanyaoSolutionSection')).toBeLessThan(page.indexOf('<TanyaoCapabilitySection'))
    expect(page.indexOf('<TanyaoCapabilitySection')).toBeLessThan(page.indexOf('tanyao-agent-title'))
    expect(page.indexOf('tanyao-agent-title')).toBeLessThan(page.indexOf('tanyao-cta-title'))

    expect(solution).toContain('ProductSystemSection')
    expect(solution).toContain('eyebrow="解决方案"')
    expect(solution).toContain('title-id="tanyao-solution-title"')
    expect(solution).toContain('ProductSystemFlowFrame')
    expect(solution).toContain('SystemCards')

    expect(capability).toContain('AlternatingTimelineSection')
    expect(capability).toContain('eyebrow="核心能力"')
    expect(capability).toContain('title-id="tanyao-capability-title"')
    expect(capability).toContain('tanyaoTimelineItems')

    // capability items have no bullets, so the shared timeline must guard the list
    expect(timeline).toContain('v-if="item.bullets.length"')

    expect(tanyaoData).toContain('export const tanyaoHeroStats: HeroStatItem[]')
    expect(tanyaoData).toContain('export const tanyaoChallenges: ProductFeatureGridItem[]')
    expect(tanyaoData).toContain('export const tanyaoSolutionCards: SystemCard[]')
    expect(tanyaoData).toContain('export const tanyaoTimelineItems: AlternatingTimelineItem[]')
    expect(tanyaoData).toContain('export const tanyaoAgents: ProductFeatureGridItem[]')
  })
})

describe('device agent page contract', () => {
  it('renders the device agent hero through the centered PageHero variant', () => {
    const hero = readComponent('components/product/device-agent/DeviceAgentHero.vue')
    const pageHero = readComponent('components/common/PageHero.vue')

    expect(hero).toContain('badge="Device Agent"')
    expect(hero).toContain('Cpu')
    expect(hero).toContain('title-id="device-agent-hero-title"')
    expect(hero).toContain('title-line="让Agent基于实时数据决策与行动"')
    expect(hero).toContain('title-gradient="串联事件、记忆、执行和决策"')
    expect(hero).toContain('visual-label="DEVICEAGENT_HORE_WEBM"')
    expect(hero).toContain('align="center"')
    expect(hero).toContain(':actions="[]"')
    expect(hero).toContain('<DeviceAgentHeroVisual />')

    // shared PageHero exposes the centered variant without breaking the left baseline
    expect(pageHero).toContain("align?: 'left' | 'center'")
    expect(pageHero).toContain("align === 'center' ? 'max-w-4xl' : 'max-w-2xl lg:self-center lg:text-left'")
    expect(pageHero).toContain("align === 'center' ? 'lg:flex lg:justify-center' : ''")
    expect(pageHero).toContain("v-if=\"align !== 'center'\" class=\"page-hero__visual-glow\"")
    expect(pageHero).toContain("flushVisualEnd ? 'lg:-mr-4 lg:justify-self-end' : ''")
  })

  it('replicates the EMQX Agents orchestrator panel structure with the six scenes', () => {
    const heroVisual = readComponent('components/product/device-agent/DeviceAgentHeroVisual.vue')

    // Tailwind-only component with SSR-safe timers
    expect(heroVisual).not.toContain('<style')
    expect(heroVisual).not.toMatch(/\sstyle=/)
    expect(heroVisual).toContain('onMounted')
    expect(heroVisual).toContain('onBeforeUnmount')

    // panel glow and frame
    expect(heroVisual).toContain('class="relative mt-10 w-full max-w-[860px]"')
    expect(heroVisual).toContain(
      'pointer-events-none absolute -inset-x-12 -bottom-4 -top-8 -z-0 rounded-[3rem] bg-primary/8 blur-3xl lg:-inset-x-24',
    )
    expect(heroVisual).toContain(
      'relative overflow-hidden rounded-2xl border border-primary/20 bg-elevated shadow-lg transition-shadow',
    )

    // ghost typing overlay with pulsing cursor plus the orchestrator textarea
    expect(heroVisual).toContain('relative min-h-[140px] px-5 pt-5 pb-3')
    expect(heroVisual).toContain(
      'pointer-events-none absolute inset-x-5 top-5 text-left text-base leading-relaxed text-muted sm:text-lg',
    )
    expect(heroVisual).toContain('ml-[2px] inline-block h-[1em] w-[1.5px] animate-pulse bg-primary align-baseline')
    expect(heroVisual).toContain('aria-label="Agent 编排器"')
    expect(heroVisual).toContain('rows="3"')
    expect(heroVisual).toContain(
      'relative w-full resize-none bg-transparent text-base leading-relaxed text-highlighted outline-none sm:text-lg',
    )

    // toolbar with the six scene buttons and the send button
    expect(heroVisual).toContain(
      'flex items-center justify-between gap-3 border-t border-primary/15 bg-dt-bg-soft/30 px-3 py-2.5',
    )
    expect(heroVisual).toContain(
      'inline-flex h-8 cursor-pointer items-center gap-1.5 rounded-full border px-3 text-xs font-medium transition-colors',
    )
    expect(heroVisual).toContain('border-primary/30 bg-primary/10 text-primary')
    expect(heroVisual).toContain('border-default bg-dt-bg/70 text-muted hover:bg-dt-bg-soft/50 hover:text-highlighted')
    expect(heroVisual).toContain('aria-pressed')
    expect(heroVisual).toContain('size-3.5')
    expect(heroVisual).toContain('hidden sm:inline')
    expect(heroVisual).toContain('aria-label="发送"')
    expect(heroVisual).toContain('ArrowUp')
    expect(heroVisual).toContain('size-4')
  })

  it('assembles the device agent page with the ecosystem grid reusing the tanyao agents', () => {
    const page = readComponent('pages/products/device-agent.vue')
    const data = readComponent('data/device-agent.ts')

    expect(page).toContain('<DeviceAgentHero />')
    expect(page).toContain('SiteHeader')
    expect(page).toContain('SiteFooter')
    expect(page).toContain('eyebrow="智能体生态"')
    expect(page).toContain('title-id="device-agent-ecosystem-title"')
    expect(page).toContain(':items="tanyaoAgents"')
    expect(page).toContain('columns="three"')
    expect(page.indexOf('<DeviceAgentHero')).toBeLessThan(page.indexOf('device-agent-ecosystem-title'))

    expect(data).toContain('export interface DeviceAgentScene')
    expect(data).toContain('export const deviceAgentScenes: DeviceAgentScene[]')
    expect(data.indexOf('事件预警')).toBeLessThan(data.indexOf('事件处置'))
    expect(data.indexOf('事件处置')).toBeLessThan(data.indexOf('工单派发'))
    expect(data.indexOf('工单派发')).toBeLessThan(data.indexOf('预测维护'))
    expect(data.indexOf('预测维护')).toBeLessThan(data.indexOf('周报汇总'))
    expect(data.indexOf('周报汇总')).toBeLessThan(data.indexOf('智能问数'))
  })

  it('renders Section2 through the shared ProductSystemSection with a flow frame placeholder', () => {
    const section = readComponent('components/product/device-agent/DeviceAgentArchitectureSection.vue')

    expect(section).toContain('ProductSystemSection')
    expect(section).toContain('eyebrow="智能体架构"')
    expect(section).toContain('title="从设备模型，到真正会行动的 Agent"')
    expect(section).toContain('title-id="device-agent-architecture-title"')
    expect(section).toContain('subtitle="Agent 理解设备能力，读取实时状态，执行指令并返回可验证的结果。"')
    expect(section).toContain('ProductSystemFlowFrame')
    expect(section).not.toContain('<style')
  })

  it('renders Section3 as three borderless numeric-icon cards and keeps FeatureCard backward compatible', () => {
    const section = readComponent('components/product/device-agent/DeviceAgentValueSection.vue')
    const featureCard = readComponent('components/common/card/FeatureCard.vue')

    expect(section).toContain('eyebrow="核心价值"')
    expect(section).toContain('title="Device Agent解决的三个关键问题"')
    expect(section).toContain('title-id="device-agent-value-title"')
    expect(section).toContain(':items="deviceAgentValueItems"')
    expect(section).toContain('columns="three"')
    expect(section).toContain(':icon-bordered="false"')

    // numeric icon labels now honor iconBordered/iconFilled without changing the default rendering
    expect(featureCard).toContain('feature-card__icon-label--borderless')
    expect(featureCard).toContain('feature-card__icon-label--unfilled')
    expect(featureCard).toContain("--dt-icon-box-shadow: none")
    expect(featureCard).toContain("--dt-icon-box-bg: transparent")
    expect(featureCard).toContain('class="relative dt-icon-box dt-icon-box--gradient text-sm font-bold"')
  })

  it('replicates the EMQX agents left-tab runtime layout with keyed panel switching', () => {
    const section = readComponent('components/product/device-agent/DeviceAgentRuntimeSection.vue')

    expect(section).not.toContain('<style')
    expect(section).toContain('title-id="device-agent-runtime-title"')
    expect(section).toContain('eyebrow="核心能力"')
    expect(section).toContain('title="为设备事件而生的 Agent Runtime"')
    expect(section).toContain('grid gap-8 lg:grid-cols-[360px_1fr] lg:gap-10')
    expect(section).toContain('deviceAgentRuntimeTabs')
    expect(section).toContain(':aria-pressed="index === activeTab"')
    expect(section).toContain('border-primary/30 bg-gradient-to-r from-primary/10 to-primary/5')
    expect(section).toContain('border-transparent bg-transparent hover:bg-dt-bg-soft/50')
    expect(section).toContain('absolute left-0 top-1/2 h-6 w-1 -translate-y-1/2 rounded-full bg-primary transition-all duration-300')
    expect(section).toContain('bg-primary/15 text-primary')
    expect(section).toContain('bg-dt-bg-soft/60 text-muted group-hover:text-highlighted')
    expect(section).toContain('group-hover:translate-x-0 group-hover:opacity-40')
    expect(section).toContain('ChevronRight')
    expect(section).toContain(':key="active?.id"')
  })

  it('keeps the runtime panel shell fixed-height with the muted container and inner default card', () => {
    const shell = readComponent('components/product/device-agent/runtime/RuntimePanelShell.vue')
    const tailwind = readComponent('assets/css/tailwind.css')

    expect(shell).not.toContain('<style')
    expect(shell).toContain('animate-panel-in')
    expect(shell).toContain(
      'relative flex h-[530px] flex-col overflow-hidden rounded-2xl border border-dt-line-strong/60 bg-dt-bg-soft/30 xl:h-[480px]',
    )
    expect(shell).toContain('flex min-h-0 flex-1 flex-col rounded-xl border border-dt-line-strong/60 bg-dt-bg p-4')
    expect(shell).toContain('rounded bg-dt-bg-soft/40 px-2 py-0.5 text-[10px] font-medium text-muted')
    expect(shell).toContain('badgeDot?: boolean')
    expect(shell).toContain('v-if="badgeDot"')
    expect(shell).toContain('v-if="$slots.footer"')
    expect(shell).toContain('relative border-t border-dt-line-strong/40 px-5 py-4')
    expect(shell).toContain('<slot name="footer" />')

    expect(tailwind).toContain('--animate-panel-in: panel-in 0.32s ease-out both')
    expect(tailwind).toContain('@keyframes panel-in')
  })

  it('uses an SSR-safe rAF timeline composable for the six runtime panels', () => {
    const timeline = readComponent('components/product/device-agent/useRuntimeTimeline.ts')

    expect(timeline).toContain('export function useRuntimeTimeline')
    expect(timeline).toContain('requestAnimationFrame')
    expect(timeline).toContain('onMounted')
    expect(timeline).toContain('onBeforeUnmount')
    expect(timeline).toContain('import.meta.client')
    expect(timeline).toContain('RUNTIME_BAR_WIDTH_CLASSES')
    expect(timeline).toContain("'w-[0%]'")
    expect(timeline).toContain("'w-[100%]'")
    expect(timeline).toContain('export function stageCount')
    expect(timeline).toContain('export const RUNTIME_LOOP_HOLD_MS')
    expect(timeline).toContain('(now - startedAt) % cycleMs')
  })

  it('keeps the ESS-01 closed loop across the six runtime panels', () => {
    const eventPanel = readComponent('components/product/device-agent/runtime/RuntimeEventPanel.vue')
    const contextPanel = readComponent('components/product/device-agent/runtime/RuntimeContextPanel.vue')
    const toolsPanel = readComponent('components/product/device-agent/runtime/RuntimeToolsPanel.vue')
    const skillsPanel = readComponent('components/product/device-agent/runtime/RuntimeSkillsPanel.vue')
    const guardrailsPanel = readComponent('components/product/device-agent/runtime/RuntimeGuardrailsPanel.vue')
    const tracePanel = readComponent('components/product/device-agent/runtime/RuntimeTracePanel.vue')

    expect(eventPanel).toContain('badge="监听中"')
    expect(eventPanel).toContain('DEVICE AGENT')
    expect(eventPanel).toContain('等待事件')
    expect(eventPanel).toContain('事件已触发')
    expect(eventPanel).toContain('61.8°C')
    expect(eventPanel).toContain('rounded-xl border border-primary/20 bg-primary/5 p-3')
    expect(eventPanel).toContain('animate-ping')
    expect(eventPanel).toContain("triggered ? '事件已触发' : '等待事件'")
    expect(eventPanel).toContain('h-1.5 flex-1 rounded-full transition-colors duration-200')
    expect(eventPanel).toContain("index < activeBars ? 'bg-primary' : 'bg-dt-bg-soft'")
    expect(eventPanel).toContain('<template #footer>')
    expect(eventPanel).toContain('max-w-xl text-sm text-muted lg:line-clamp-2')
    expect(eventPanel).toContain('验证来源与事件结构')
    expect(eventPanel).toContain('关联设备与历史上下文')
    expect(eventPanel).toContain('ArrowRight')
    expect(eventPanel).toContain('animate-pulse')
    expect(eventPanel).toContain("import IconBox from '~/components/common/card/IconBox.vue'")
    expect(eventPanel).toContain("stepState(index) === 'pending' ? 'soft' : 'primary'")
    expect(eventPanel).toContain('truncate text-sm font-semibold text-highlighted')
    expect(eventPanel).toContain('mt-1 hidden truncate text-xs leading-5 text-muted xl:block')
    expect(eventPanel).toContain('mt-5 rounded-xl border border-dt-line-strong/60 bg-dt-bg p-2.5')
    expect(eventPanel).toContain('flex items-baseline justify-between gap-2 text-xs leading-4 transition-opacity duration-500')
    expect(eventPanel).toContain('mb-3 flex items-center gap-2')
    expect(eventPanel).toContain('grid gap-2')
    expect(eventPanel).toContain('mt-5 flex items-stretch gap-1')

    expect(contextPanel).toContain('title="设备上下文引擎"')
    expect(contextPanel).toContain('badge="实时构建"')
    expect(contextPanel).toContain('ESS-01 设备上下文')
    expect(contextPanel).toContain('上下文构建序列')
    expect(contextPanel).toContain('context.build({ device: "ESS-01", sources: 4 })')
    expect(contextPanel).toContain('Context Ready · 1.2s')
    expect(contextPanel).toContain("import IconBox from '~/components/common/card/IconBox.vue'")
    expect(contextPanel).toContain('shrink-0 rounded-full bg-primary/10 px-2.5 py-1 text-xs font-medium text-primary')
    expect(contextPanel).toContain('mt-5 grid min-h-0 flex-1 grid-cols-2 gap-2 sm:gap-3 xl:grid-cols-4 xl:flex-none')
    expect(contextPanel).toContain('mt-5 flex items-center gap-2')
    expect(contextPanel).toContain('h-1.5 flex-1 rounded-full transition-colors duration-200')
    expect(contextPanel).toContain("sourceState(card) === 'idle' ? 'bg-dt-bg-soft' : 'bg-primary'")
    expect(contextPanel).toContain('flex h-full items-center gap-2 rounded-lg border p-2.5 transition-all duration-300')
    expect(contextPanel).toContain('truncate text-sm font-semibold leading-5 text-highlighted')
    expect(contextPanel).toContain('mt-0.5 truncate text-xs leading-4 text-muted')
    expect(contextPanel).toContain('mt-1 truncate font-mono text-xs leading-4')
    expect(contextPanel).toContain('设备实时运行指标')
    expect(contextPanel).toContain('历史趋势与运行基线')
    expect(contextPanel).toContain('设备及子系统状态')
    expect(contextPanel).toContain('运维知识与处置规范')
    expect(contextPanel).toContain('mt-5 rounded-xl border border-dt-line-strong/60 bg-dt-bg p-2.5')
    expect(contextPanel).toContain('truncate text-sm text-muted')
    expect(contextPanel).not.toContain('知识参考')
    expect(contextPanel).not.toContain('mt-auto')

    expect(toolsPanel).toContain('title="MCP 工具连接"')
    expect(toolsPanel).toContain('badge="6 个可用"')
    expect(toolsPanel).toContain('badge-dot')
    expect(toolsPanel).toContain('timeseries.query')
    expect(toolsPanel).toContain('knowledge.search')
    expect(toolsPanel).toContain('查询历史时序')
    expect(toolsPanel).toContain('查询知识库')
    expect(toolsPanel).toContain('找到 3 条相关知识')
    expect(toolsPanel).toContain('useRuntimeTimeline(7200)')
    expect(toolsPanel).toContain('<template #footer>')
    expect(toolsPanel).toContain('Agent 通过 MCP 协议连接设备、数据与业务工具，在消息流中完成读取、查询、检索与执行。')
    expect(toolsPanel).toContain('创建储能运维工单')

    expect(skillsPanel).toContain('badge="按需加载"')
    expect(skillsPanel).toContain('正在匹配 Skills...')
    expect(skillsPanel).toContain('已挂载 3 个 Skills · 就绪')

    expect(guardrailsPanel).toContain('badge="护栏已启用"')
    expect(guardrailsPanel).toContain('device.execute_command')
    expect(guardrailsPanel).toContain('需要人工审批')
    expect(guardrailsPanel).toContain('执行确认')

    expect(tracePanel).toContain('badge="采集中"')
    expect(tracePanel).toContain('deviceAgentTraceSteps')
    expect(tracePanel).toContain('重放本次运行')
    expect(tracePanel).toContain('@click="restart()"')

    for (const panel of [eventPanel, contextPanel, toolsPanel, skillsPanel, guardrailsPanel, tracePanel]) {
      expect(panel).not.toContain('<style')
      expect(panel).toContain('RuntimePanelShell')
      expect(panel).toContain('useRuntimeTimeline')
    }
  })

  it('assembles the device agent page sections in the required order', () => {
    const page = readComponent('pages/products/device-agent.vue')

    expect(page).toContain('<DeviceAgentArchitectureSection />')
    expect(page).toContain('<DeviceAgentValueSection />')
    expect(page).toContain('<DeviceAgentRuntimeSection />')
    expect(page.indexOf('device-agent-ecosystem-title')).toBeLessThan(page.indexOf('<DeviceAgentArchitectureSection'))
    expect(page.indexOf('<DeviceAgentArchitectureSection')).toBeLessThan(page.indexOf('<DeviceAgentValueSection'))
    expect(page.indexOf('<DeviceAgentValueSection')).toBeLessThan(page.indexOf('<DeviceAgentRuntimeSection'))
  })
})
